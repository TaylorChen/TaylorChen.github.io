<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="zh-CN"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="http://localhost:4000/feed.xml" rel="self" type="application/atom+xml" /><link href="http://localhost:4000/" rel="alternate" type="text/html" hreflang="zh-CN" /><updated>2026-06-13T14:37:22+08:00</updated><id>http://localhost:4000/feed.xml</id><title type="html">小黄鸭</title><subtitle>这里是 小黄鸭 的个人博客，与你一起发现更大的世界 | 要做一个有 ikigai 的程序员</subtitle><author><name>小黄鸭</name></author><entry><title type="html">上下文工程：比 Prompt 更重要的那一层</title><link href="http://localhost:4000/ai/%E6%8A%80%E6%9C%AF/2026/06/13/%E4%B8%8A%E4%B8%8B%E6%96%87%E5%B7%A5%E7%A8%8B-%E6%AF%94Prompt%E6%9B%B4%E9%87%8D%E8%A6%81%E7%9A%84%E9%82%A3%E4%B8%80%E5%B1%82.html" rel="alternate" type="text/html" title="上下文工程：比 Prompt 更重要的那一层" /><published>2026-06-13T00:00:00+08:00</published><updated>2026-06-13T08:13:49+08:00</updated><id>http://localhost:4000/ai/%E6%8A%80%E6%9C%AF/2026/06/13/%E4%B8%8A%E4%B8%8B%E6%96%87%E5%B7%A5%E7%A8%8B-%E6%AF%94Prompt%E6%9B%B4%E9%87%8D%E8%A6%81%E7%9A%84%E9%82%A3%E4%B8%80%E5%B1%82</id><content type="html" xml:base="http://localhost:4000/ai/%E6%8A%80%E6%9C%AF/2026/06/13/%E4%B8%8A%E4%B8%8B%E6%96%87%E5%B7%A5%E7%A8%8B-%E6%AF%94Prompt%E6%9B%B4%E9%87%8D%E8%A6%81%E7%9A%84%E9%82%A3%E4%B8%80%E5%B1%82.html"><![CDATA[<p>2023 年大家卷 Prompt 工程，2025 年之后行业共识逐渐转向一个更大的概念：上下文工程（Context Engineering）。两者的区别一句话能说清——Prompt 工程优化「你怎么对模型说话」，上下文工程管理「模型做决策的那一刻，窗口里到底有什么」。</p>

<p>单轮问答时代两者几乎是一回事。但当任务变成长程 Agent——跑几十分钟、调几百次工具、处理几万行代码——窗口里的内容 95% 不再是你写的 prompt，而是系统注入的工具定义、检索结果、历史消息、文件内容。这时候决定产出质量的，是这 95% 的管理水平。</p>

<h2 id="上下文是稀缺资源要当预算管">上下文是稀缺资源，要当预算管</h2>

<p>先建立一个核心心智：<strong>上下文窗口不是仓库，是工作台</strong>。仓库越大越好，工作台不是——堆满杂物的工作台干不了精细活。</p>

<p>这不只是比喻。实证上有两个效应：一是「迷失在中间」（lost in the middle），窗口中部的信息被利用的概率显著低于首尾；二是上下文腐烂（context rot），随着无关信息占比上升，模型遵循指令的能力整体退化，哪怕关键信息明明还在窗口里。窗口标称 100 万 token，不代表你应该塞 100 万——有效注意力远小于标称容量。</p>

<p>所以上下文工程的本质是预算管理：每一段进入窗口的内容,都要回答「它在这个决策时刻的边际价值，配得上它占的位置吗」。</p>

<p><img src="/assets/images/2026/context-budget.svg" alt="上下文预算分配" /></p>

<h2 id="四个操作写入选取压缩隔离">四个操作：写入、选取、压缩、隔离</h2>

<p>业内逐渐收敛出四个基本操作，几乎所有上下文工程技巧都是它们的组合。</p>

<p><strong>写入（Write）：把信息移出窗口，存到外部。</strong> Agent 的中间结论、计划、待办，写到文件或状态存储里，而不是依赖对话历史。典型实现是让 Agent 维护一个 <code class="language-plaintext highlighter-rouge">notes.md</code> / 任务清单文件——窗口会被挤爆，文件不会。跨会话的记忆系统也属于这类。</p>

<p><strong>选取（Select）：按需拉回来。</strong> 决策时刻只注入相关的部分。RAG 是选取，工具的动态裁剪是选取（一百个工具只注入语义相关的十个），Agent 主动 <code class="language-plaintext highlighter-rouge">grep</code>/<code class="language-plaintext highlighter-rouge">read</code> 文件也是选取。选取的关键指标不是召回率，是<strong>信噪比</strong>——多拉进来的每一段无关内容都在划伤工作台。</p>

<p><strong>压缩（Compact）：有损地缩小占用。</strong> 对话历史超过阈值时做摘要压缩；工具返回的 5000 行 JSON 先裁剪成关键字段再入窗。压缩的风险是丢掉后来才知道重要的细节，所以原则是：可再生的信息（随时能重新读到的文件内容）激进压缩，不可再生的信息（一次性的执行结果、用户的口头约束）保守压缩。</p>

<p><strong>隔离（Isolate）：拆给子上下文。</strong> 把高消耗的子任务委托给 sub-agent，它在自己的窗口里烧几十万 token，最后只把三段结论带回主窗口。上一篇讲的多 Agent 协作，从上下文工程视角看就是隔离操作的系统化。</p>

<h2 id="一个真实的调优案例">一个真实的调优案例</h2>

<p>我们一个内部代码审查 Agent，初版效果很差：审到第 30 个文件时开始胡言乱语，把第 5 个文件的问题安到第 28 个文件头上。排查后发现窗口已被三类东西塞满：全量工具定义（18k token）、每个文件的完整内容（平均 6k×30）、以及越滚越长的审查历史。</p>

<p>按四操作改造：</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>写入  审查发现实时写入 findings.json，窗口里只保留
      「当前文件 + 最近 3 条发现的摘要」
选取  文件内容不再全量注入，先注入结构骨架（函数签名+行数），
      Agent 按需读具体函数体
压缩  每审完 10 个文件做一次历史压缩，保留结论丢弃过程
隔离  跨文件的一致性检查拆给独立 sub-agent，
      输入是 findings.json 而非对话历史
</code></pre></div></div>

<p>改造后单文件审查的窗口占用从持续增长变成稳定在 25k token 左右，30 文件之后的准确率和前 5 个文件无显著差异。值得注意的是：模型没换、prompt 几乎没动，<strong>纯上下文结构的改造带来了从不可用到可用的跨越</strong>。这就是我说「比 Prompt 更重要」的依据。</p>

<h2 id="给上下文做监控">给上下文做监控</h2>

<p>工程化的标志是可度量。我们现在给所有 Agent 加了三个上下文指标：</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@dataclass</span>
<span class="k">class</span> <span class="nc">ContextMetrics</span><span class="p">:</span>
    <span class="n">window_usage</span><span class="p">:</span> <span class="nb">float</span>      <span class="c1"># 窗口占用率，&gt;70% 触发压缩
</span>    <span class="n">composition</span><span class="p">:</span> <span class="nb">dict</span>        <span class="c1"># 构成比例：指令/工具/历史/检索各占多少
</span>    <span class="n">staleness</span><span class="p">:</span> <span class="nb">float</span>         <span class="c1"># 陈旧度：窗口中多少 token 与最近
</span>                             <span class="c1"># 5 次决策无关（采样评估）
</span></code></pre></div></div>

<p>构成比例（composition）最有诊断价值。健康的长程 Agent 大致是：系统指令 5~10%、工具定义 10~15%、工作记忆（计划+近期历史）30~40%、当前任务素材 30~40%、预留 15~20%。如果发现工具定义占了 35%，不用看效果就知道该做工具裁剪了。</p>

<h2 id="几个容易踩的反模式">几个容易踩的反模式</h2>

<p>落地过程中见过的典型错误，列出来供对照自查。</p>

<p><strong>反模式一：把系统提示词当垃圾抽屉。</strong> 每次出 bad case 就往 system prompt 里加一条规则，半年后系统提示词膨胀到八千 token，规则之间互相打架，模型无所适从。正确做法是定期重构提示词，就像重构代码一样——合并同类规则、删除已被模型默认行为覆盖的规则，把场景化的规则移到 Skill 里按需加载。</p>

<p><strong>反模式二：检索结果不设预算上限。</strong> RAG 召回 top-k 的 k 拍脑袋设成 20，每段 chunk 500 token，单次检索就吃掉一万 token，其中真正相关的往往只有前三段。检索预算应该从答案质量倒推：先用小 k 跑评测集，k 加大到指标不再提升就停——多数场景这个拐点出现在 3~5。</p>

<p><strong>反模式三：用「重新开一个会话」代替压缩策略。</strong> 窗口满了就让用户开新会话，所有上下文清零重来。这是把成本转嫁给了用户——他们要手动复述背景。哪怕最朴素的自动压缩（保留系统指令 + 最近 N 轮 + 一段历史摘要）都比清零体验好得多。</p>

<p><strong>反模式四：只在出问题时才看上下文。</strong> 上下文构成应该和延迟、错误率一样进日常监控面板。腐烂是渐进的，等用户投诉”AI 变笨了”再去看，往往已经烂了几周。</p>

<h2 id="和-skillharness-的关系">和 Skill、Harness 的关系</h2>

<p>把这半年聊过的几个概念串起来：Harness 是 Agent 的执行框架（循环、工具、权限），Skill 是按需加载的能力包，上下文工程是贯穿其中的资源管理纪律。Skill 的「渐进式披露」——先给模型看一行摘要，需要时才加载全文——本身就是上下文工程里「选取」操作的优雅实现。这些东西不是并列的三个 buzzword，是同一套系统的三个切面：<strong>Harness 定义边界，Skill 提供能力，上下文工程决定单位注意力的产出效率。</strong></p>

<h2 id="结语">结语</h2>

<p>回头看这几年的路线，从优化一句话（Prompt），到优化一次调用的输入（RAG），再到管理一个长程系统里信息的全生命周期，方向是一致的：把炼丹一步步变成工程，可度量、可监控、可优化。</p>

<p>如果只能带走一条：下次 Agent 表现退化，先别急着改 prompt 或者换模型，把那一刻的窗口内容完整 dump 出来，自己从头读一遍。我每次这么做都会看到一个被工具返回值、过期历史和无关检索塞满的工作台，然后改法自然就出来了。这个笨办法教给过好几个同事，没有一次落空。</p>]]></content><author><name>小黄鸭</name></author><category term="AI" /><category term="技术" /><category term="AI" /><category term="上下文工程" /><category term="Context Engineering" /><category term="Agent" /><category term="工程实践" /><summary type="html"><![CDATA[Prompt 工程优化的是「怎么说」，上下文工程管理的是「模型在决策时刻能看到什么」。当任务从单轮问答变成长程 Agent，后者才是质量的决定因素。本文给出一套可落地的上下文预算管理方法。]]></summary></entry><entry><title type="html">多 Agent 系统的协作模式：从单兵到军团</title><link href="http://localhost:4000/ai/%E6%8A%80%E6%9C%AF/2026/06/06/%E5%A4%9AAgent%E7%B3%BB%E7%BB%9F%E7%9A%84%E5%8D%8F%E4%BD%9C%E6%A8%A1%E5%BC%8F-%E4%BB%8E%E5%8D%95%E5%85%B5%E5%88%B0%E5%86%9B%E5%9B%A2.html" rel="alternate" type="text/html" title="多 Agent 系统的协作模式：从单兵到军团" /><published>2026-06-06T00:00:00+08:00</published><updated>2026-06-13T08:13:39+08:00</updated><id>http://localhost:4000/ai/%E6%8A%80%E6%9C%AF/2026/06/06/%E5%A4%9AAgent%E7%B3%BB%E7%BB%9F%E7%9A%84%E5%8D%8F%E4%BD%9C%E6%A8%A1%E5%BC%8F-%E4%BB%8E%E5%8D%95%E5%85%B5%E5%88%B0%E5%86%9B%E5%9B%A2</id><content type="html" xml:base="http://localhost:4000/ai/%E6%8A%80%E6%9C%AF/2026/06/06/%E5%A4%9AAgent%E7%B3%BB%E7%BB%9F%E7%9A%84%E5%8D%8F%E4%BD%9C%E6%A8%A1%E5%BC%8F-%E4%BB%8E%E5%8D%95%E5%85%B5%E5%88%B0%E5%86%9B%E5%9B%A2.html"><![CDATA[<p>2025 年是单 Agent 成熟的一年，2026 年的主战场明显转向了多 Agent。但「多」不等于「强」——我见过太多团队把三个 Agent 串起来，效果还不如调教好的一个。多 Agent 系统的能力上限不取决于 Agent 数量，取决于协作结构的设计。这篇文章拆解四种主流协作模式、各自的适用边界，以及那个最大的暗坑。</p>

<h2 id="为什么需要多-agent">为什么需要多 Agent</h2>

<p>先回答前置问题：单 Agent 不够用吗？很多场景确实不够，原因有三。</p>

<p><strong>上下文窗口是物理上限。</strong> 一个复杂任务（比如重构一个中型代码库）所需的上下文远超窗口容量，单 Agent 越往后跑，早期信息被挤出，质量塌方。拆成多个 Agent，每个只装自己需要的上下文，是对窗口限制的工程化绕行。</p>

<p><strong>角色专注度。</strong> 同一个模型，给它「你是测试工程师，只挑毛病」的窄指令，比「你又写又测」的宽指令表现更好。注意力被指令聚焦了。这和人类组织分工的逻辑相同。</p>

<p><strong>并行加速。</strong> 互不依赖的子任务（扫描十个模块找安全漏洞）天然适合并行,十个 Agent 同时跑就是十倍吞吐。</p>

<p>但这三个收益都有代价，代价就藏在协作模式里。</p>

<h2 id="四种协作模式">四种协作模式</h2>

<p><img src="/assets/images/2026/multi-agent-patterns.svg" alt="多 Agent 协作模式" /></p>

<p><strong>模式一：Orchestrator-Worker（主从编排）。</strong> 一个主 Agent 负责理解任务、拆解、分发给 Worker、汇总结果。这是目前生产里最主流的模式，Claude 的 sub-agent、各家 Deep Research 产品都是这个结构。优点是控制流清晰、易调试；缺点是 Orchestrator 是单点——它拆错了任务，下面全白干。适用：任务可分解、子任务边界清晰的场景（研究、代码库分析、批量处理）。</p>

<p><strong>模式二：Pipeline（流水线）。</strong> Agent 按固定顺序接力：需求分析 → 编码 → 测试 → 审查。每个 Agent 的输出是下一个的输入。优点是质量门禁明确，每一环都可以独立优化和评测；缺点是错误会沿管道放大，且总延迟是各环节之和。适用：流程固定、每步产物可验证的场景（内容生产、代码 CI 流、数据 ETL）。</p>

<p><strong>模式三：Debate（辩论/对抗）。</strong> 两个以上 Agent 持不同立场互相批驳，第三方裁决或收敛共识。研究表明对抗结构能显著降低单模型的「自信幻觉」。成本是 token 翻几倍。适用：高风险判断类任务——安全审查、方案评审、事实核查，不适用于产出型任务。</p>

<p><strong>模式四：Swarm（群体投票）。</strong> 同一任务发给 N 个独立 Agent，对结果投票或聚合。本质是用冗余换可靠性，类似分布式系统的多数派。适用：答案可比较、正确性可投票的封闭性问题（分类、打分、数学题）；开放性产出（写一篇文档）没法投票，不适用。</p>

<p>实践中的组合拳通常是：Orchestrator 做骨架，关键节点插入 Debate 做质检，可并行处插 Swarm。</p>

<h2 id="最大的暗坑上下文割裂">最大的暗坑：上下文割裂</h2>

<p>多 Agent 系统第一大故障源不是模型能力，是<strong>信息在 Agent 之间传递时的损耗</strong>。</p>

<p>单 Agent 的所有决策共享同一份上下文；拆成多 Agent 后,每个 Worker 只看到 Orchestrator 转述的任务描述。转述必然有损：Worker A 在模块里发现的一个重要约束，Worker B 不知道；Orchestrator 汇总时把两个互相矛盾的结论拼在了一起，自己却看不出矛盾——因为支撑各自结论的证据都没传上来。</p>

<p>这就是为什么很多多 Agent 系统的产出「每一段都对，整体是错的」。</p>

<p>工程上的缓解手段，按性价比排序：</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1. 共享黑板（Blackboard）：所有 Agent 读写同一份结构化状态
   文件/数据库，关键发现必须落盘，而不是只存在于对话里
2. 任务描述带上下文契约：Orchestrator 分发任务时显式附带
   「已知约束」「禁止假设」「输出 schema」三段
3. 汇总前做一致性检查：专设一个 Agent 校对各 Worker 结论
   之间的冲突，冲突即打回重跑
4. 控制扇出深度：层级不超过两层，Agent 套 Agent 套 Agent
   的转述损耗是指数级的
</code></pre></div></div>

<p>一个简化的 Orchestrator 骨架（基于消息而非框架，刻意展示裸逻辑）：</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">async</span> <span class="k">def</span> <span class="nf">orchestrate</span><span class="p">(</span><span class="n">task</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">Report</span><span class="p">:</span>
    <span class="n">plan</span> <span class="o">=</span> <span class="k">await</span> <span class="nf">llm</span><span class="p">(</span><span class="n">PLANNER</span><span class="p">,</span> <span class="n">task</span><span class="p">)</span>                  <span class="c1"># 拆解
</span>    <span class="n">board</span> <span class="o">=</span> <span class="nc">Blackboard</span><span class="p">()</span>                             <span class="c1"># 共享黑板
</span>    <span class="n">results</span> <span class="o">=</span> <span class="k">await</span> <span class="n">asyncio</span><span class="p">.</span><span class="nf">gather</span><span class="p">(</span><span class="o">*</span><span class="p">[</span>
        <span class="nf">run_worker</span><span class="p">(</span><span class="n">sub</span><span class="p">,</span> <span class="n">board</span><span class="p">.</span><span class="nf">snapshot</span><span class="p">())</span>            <span class="c1"># 带上下文契约分发
</span>        <span class="k">for</span> <span class="n">sub</span> <span class="ow">in</span> <span class="n">plan</span><span class="p">.</span><span class="n">subtasks</span>
    <span class="p">])</span>
    <span class="k">for</span> <span class="n">r</span> <span class="ow">in</span> <span class="n">results</span><span class="p">:</span>
        <span class="n">board</span><span class="p">.</span><span class="nf">merge</span><span class="p">(</span><span class="n">r</span><span class="p">.</span><span class="n">findings</span><span class="p">)</span>                      <span class="c1"># 关键发现落盘
</span>    <span class="n">conflicts</span> <span class="o">=</span> <span class="k">await</span> <span class="nf">llm</span><span class="p">(</span><span class="n">CHECKER</span><span class="p">,</span> <span class="n">board</span><span class="p">.</span><span class="nf">dump</span><span class="p">())</span>     <span class="c1"># 一致性检查
</span>    <span class="k">if</span> <span class="n">conflicts</span><span class="p">:</span>
        <span class="n">results</span> <span class="o">+=</span> <span class="k">await</span> <span class="nf">rerun</span><span class="p">(</span><span class="n">conflicts</span><span class="p">,</span> <span class="n">board</span><span class="p">)</span>     <span class="c1"># 冲突打回
</span>    <span class="k">return</span> <span class="k">await</span> <span class="nf">llm</span><span class="p">(</span><span class="n">SYNTHESIZER</span><span class="p">,</span> <span class="n">board</span><span class="p">.</span><span class="nf">dump</span><span class="p">())</span>      <span class="c1"># 基于黑板汇总
</span></code></pre></div></div>

<p>注意汇总（SYNTHESIZER）的输入是黑板而不是各 Worker 的原始对话——这是刻意的。让汇总者面对结构化的事实清单，而不是 N 段冗长转述，幻觉率会明显下降。</p>

<h2 id="怎么评测一个多-agent-系统">怎么评测一个多 Agent 系统</h2>

<p>多 Agent 系统的评测比单 Agent 难，因为失败可能发生在任何一环，端到端指标好坏都说明不了原因。我们的做法是三层评测。</p>

<p><strong>单元层：每个 Agent 独立评测。</strong> 给每个角色（规划、执行、检查、汇总）建立独立的评测集，固定输入测输出。Worker 的评测集最好直接从线上失败案例里攒——Orchestrator 拆出来的真实子任务，比手造的测试用例有效得多。</p>

<p><strong>链路层：注入故障测鲁棒性。</strong> 刻意让某个 Worker 返回错误结论、超时、返回格式损坏的输出，观察系统是否能识别和恢复。多 Agent 系统和分布式系统一样，部分失败是常态而不是异常，没测过故障注入的系统等于没测过。</p>

<p><strong>端到端层：人工评估抽样 + 配对对比。</strong> 终态产出用人工抽样打分，并且永远和单 Agent 基线做配对对比——同一批任务，单 Agent 调到最好 vs 多 Agent 系统，盲评。这一步经常给人泼冷水：相当比例的场景里，精心调优的单 Agent 能追平复杂的多 Agent 编排，而成本只有几分之一。这个对比实验做不做，决定了你的架构决策是基于证据还是基于时髦。</p>

<h2 id="成本与可观测性">成本与可观测性</h2>

<p>多 Agent 的 token 消耗通常是单 Agent 的 3~15 倍，上生产前必须算账：这个任务质量提升带来的价值，覆盖得了成本吗？我的粗规则：日常高频任务用单 Agent 调到极致；低频高价值任务（深度研究、全库审计、安全评审）才值得上多 Agent。</p>

<p>可观测性是另一个必须前置的投入。多 Agent 跑挂了，你需要知道是哪个 Agent、在哪一步、基于什么输入做错的。最低配置：每个 Agent 调用记录输入/输出/耗时/token 的 trace，全链路一个 trace id 串起来。OpenTelemetry 的 GenAI 语义约定已经够用，别等出了事故再补。</p>

<h2 id="结语">结语</h2>

<p>多 Agent 系统在重演分布式系统的老路：单机不够了才分布式，然后一致性、可观测性、部分失败这些问题排着队找上门。判断标准也可以直接抄分布式那套——能单 Agent 解决的别拆；确实要拆，先设计信息流（谁知道什么、怎么同步），再设计控制流（谁指挥谁）。</p>

<p>我们自己的代码审查系统折腾了三轮，最后稳定在「Orchestrator + 黑板 + 一个一致性检查角色」这个不算花哨的结构上。中间试过五层 Agent 嵌套的版本，看 demo 很酷，上线一周就退回来了——出了问题没人说得清是哪一层错的。供参考。</p>]]></content><author><name>小黄鸭</name></author><category term="AI" /><category term="技术" /><category term="AI" /><category term="Agent" /><category term="多Agent" /><category term="架构设计" /><category term="工程实践" /><summary type="html"><![CDATA[单 Agent 的能力上限是上下文窗口，多 Agent 的能力上限是协作设计。本文拆解 Orchestrator、流水线、辩论、群体投票四种主流协作模式的适用边界，以及多 Agent 系统最大的暗坑：上下文割裂。]]></summary></entry><entry><title type="html">AI 时代的深度工作：注意力是最后的稀缺资源</title><link href="http://localhost:4000/%E6%80%9D%E8%80%83/2026/05/31/AI%E6%97%B6%E4%BB%A3%E7%9A%84%E6%B7%B1%E5%BA%A6%E5%B7%A5%E4%BD%9C-%E6%B3%A8%E6%84%8F%E5%8A%9B%E6%98%AF%E6%9C%80%E5%90%8E%E7%9A%84%E7%A8%80%E7%BC%BA%E8%B5%84%E6%BA%90.html" rel="alternate" type="text/html" title="AI 时代的深度工作：注意力是最后的稀缺资源" /><published>2026-05-31T00:00:00+08:00</published><updated>2026-06-13T08:13:31+08:00</updated><id>http://localhost:4000/%E6%80%9D%E8%80%83/2026/05/31/AI%E6%97%B6%E4%BB%A3%E7%9A%84%E6%B7%B1%E5%BA%A6%E5%B7%A5%E4%BD%9C-%E6%B3%A8%E6%84%8F%E5%8A%9B%E6%98%AF%E6%9C%80%E5%90%8E%E7%9A%84%E7%A8%80%E7%BC%BA%E8%B5%84%E6%BA%90</id><content type="html" xml:base="http://localhost:4000/%E6%80%9D%E8%80%83/2026/05/31/AI%E6%97%B6%E4%BB%A3%E7%9A%84%E6%B7%B1%E5%BA%A6%E5%B7%A5%E4%BD%9C-%E6%B3%A8%E6%84%8F%E5%8A%9B%E6%98%AF%E6%9C%80%E5%90%8E%E7%9A%84%E7%A8%80%E7%BC%BA%E8%B5%84%E6%BA%90.html"><![CDATA[<p>卡尔·纽波特写《深度工作》是 2016 年，那时的敌人是 Slack 和推送通知。十年后的今天，敌人换了——是那个随叫随到、永远在线、回答一切的 AI 助手。这听起来很怪：AI 不是来帮我们的吗？是，但它同时制造了一种新型的注意力碎片化，而且比通知更隐蔽。</p>

<h2 id="新的碎片化等待与切换的缝隙">新的碎片化：等待与切换的缝隙</h2>

<p>观察一下自己用 AI 编程的真实节奏：下达任务，Agent 跑两分钟，这两分钟干什么？大多数人会切去看消息、刷一眼群、开第二个任务。等 Agent 跑完，你已经离开了刚才的思维现场，要花几十秒甚至几分钟「重新加载」上下文。</p>

<p>这种模式有个特点：每个间隙都「短得不值得深入，长得无法忽略」。它不像写代码被打断那样有明显的痛感，反而带着一种「我在并行处理、效率很高」的错觉。但认知科学的结论很无情：人类不存在真正的并行深度思考，只有快速切换，而每次切换都有损耗——残留注意力（attention residue）会让你在新任务上的表现下降，哪怕你自我感觉良好。</p>

<p>更隐蔽的是依赖性碎片化：遇到一点点不确定就问 AI，得到答案再继续。每一次「问一下」都是一次思维中断。以前我们查文档前会先想十秒，现在连这十秒都省了。省掉的不是十秒，是形成自己判断的那个过程。</p>

<h2 id="瓶颈的转移">瓶颈的转移</h2>

<p>为什么这件事在 2026 年特别值得谈？因为瓶颈转移了。</p>

<p>AI 之前，工程师的产出公式约等于「思考质量 × 执行速度」，执行占大头——一个想法要变成上线的代码，中间隔着几天的体力活。所以行业奖励手快的人。</p>

<p>AI 把执行成本压到接近于零之后，公式变成了「思考质量 × 判断次数」。Agent 一晚上能产出几千行代码，方向错了就是几千行的垃圾。你的杠杆不再是写得快，而是想得清：问题定义对不对、方案的约束找全没有、AI 产出的东西该不该收。</p>

<p>而「想得清」恰恰是深度工作的产物。它需要不被打断的、连续的、有些枯燥的专注时段。也就是说：<strong>AI 越强，执行越廉价，深度思考的相对价值就越高</strong>——这是被很多「效率内容」完全说反的一点。他们教你怎么同时开五个 Agent，却不教你怎么保住那个决定五个 Agent 是否在做正确事情的脑子。</p>

<h2 id="我调整后的工作结构">我调整后的工作结构</h2>

<p>过去半年我重构了自己的工作模式，核心是把「与 AI 协作」也纳入深度/浅度的二分法管理。</p>

<p><strong>深度时段不开 Agent。</strong> 每天上午保留两小时，只做问题定义、方案设计、架构权衡这类「上游思考」，纸笔或白板优先。这个时段的产出物是清晰的任务描述和判断标准——它们随后会成为 Agent 的输入。讽刺的是，这个不用 AI 的时段，恰恰是 AI 产出质量的决定因素：你给 Agent 的 spec 有多清楚，它的产出就有多能用。</p>

<p><strong>批处理 Agent 任务。</strong> 把可以委托的任务攒成批，集中在固定时段发出去，让多个 Agent 并行跑长任务（一小时以上的那种），而不是发一个等一个。等待期不再是「缝隙」，因为我此时整块地切去做另一件深度事务，回来后集中验收。关键区别：切换是计划内的、以小时为单位的，而不是被动的、以分钟为单位的。</p>

<p><strong>给「问 AI」设置十秒规则。</strong> 遇到问题，先强迫自己用十秒形成一个预期答案，再去问。这十秒保住了两样东西：自己的判断肌肉，以及对 AI 答案的校验能力——你有了预期，才能在 AI 胡说时立刻闻出味儿来。没有预期的提问，得到的答案你只能全盘接受。</p>

<p><strong>验收是深度工作，不是浅度工作。</strong> 这是最容易出错的地方。Review AI 产出的代码看起来像浅活儿，划划屏幕点点头。但 AI 产出的错误往往是「局部完美、全局错位」型的——每个函数都对，组合起来方向不对。这种错误只有带着完整上下文深读才能发现。我现在把大块验收安排在精力好的时段，和写设计文档同等对待。</p>

<h2 id="一周的自我观测实验">一周的自我观测实验</h2>

<p>道理讲完了，给一组自己的数据。今年三月我做了两周对照实验：第一周按旧习惯工作（Agent 随用随发、消息随到随看），第二周按上面的结构执行。每天记录三个指标：深度时段总时长（连续 25 分钟以上无切换才计入）、任务返工次数（AI 产出验收不通过需要重派的次数）、主观精力评分。</p>

<p>结果比我预期的更明显。旧习惯那一周，日均深度时长只有 1.2 小时——我自以为「整天都在工作」，但符合深度标准的时间少得可怜，绝大多数时间花在了切换、等待和碎片化的对话里。返工率是另一个刺眼的数字：随手发出去的 Agent 任务，41% 需要返工；而第二周经过上午深度时段打磨过 spec 的任务，返工率降到 12%。换句话说，<strong>在任务定义上多花的二十分钟，平均省下了一个多小时的返工与扯皮</strong>。</p>

<p>第二周的日程骨架大概是这样：9:00–11:00 深度时段（关机状态，只许纸笔和编辑器）；11:00–11:30 批量派发 Agent 任务，通常 3~5 个；11:30–12:30 处理消息与会议；下午两个 90 分钟块，一个用于验收与深读 AI 产出，一个用于第二优先级的深度事务；Agent 在后台跑它们的，我不看进度条。这个结构最大的反直觉之处是：<strong>AI 的使用时长下降了，AI 的有效产出反而上升了</strong>。</p>

<p>当然，两周的自我实验谈不上严谨，样本是一个人，任务也不完全可比。但趋势与认知科学的既有结论一致，我愿意按这个方向继续押注。</p>

<h2 id="常见的两个反驳">常见的两个反驳</h2>

<p><strong>「多 Agent 并行不就是为了榨干等待时间吗？你这是开倒车。」</strong> 不矛盾。并行本身没问题，问题在并行的粒度。以分钟为单位、被 Agent 进度驱动的被动切换是碎片化；以小时为单位、按自己节奏计划的主动切换是批处理。前者你是 Agent 的随从，后者你是调度者。区别不在并行数量，在谁掌握切换的主导权。</p>

<p><strong>「初级工程师没有那么多上游思考可做，这套结构是不是只适合资深的人？」</strong> 恰恰相反，越早建立越好。初级阶段的深度时段可以用来做另一件事：深读 AI 产出，搞懂每一行为什么这么写。AI 时代初级工程师最大的危险不是被替代，而是从未真正写过、读懂过足够多的代码，导致判断力永远没机会长出来。深度时段就是判断力的健身房，资历越浅越需要。</p>

<h2 id="防住认知外包的滑坡">防住「认知外包」的滑坡</h2>

<p>最后说一个更长期的担忧：认知外包的滑坡效应。</p>

<p>用进废退在认知能力上同样成立。如果所有难题第一反应都是丢给 AI，三年后你会发现自己丧失了「在没有答案提示的情况下硬想」的能力。而这个能力恰恰是处理全新问题——AI 训练数据里没有的问题——的唯一依靠。</p>

<p>我的对策不是少用 AI，而是刻意保留「徒手区」：每周留一些问题强制自己独立解决，像健身一样维持认知肌肉。读完 AI 的方案后强迫自己说出「它为什么这么做、我原本会怎么做、差异在哪」，把每次委托变成一次对照学习而不是一次卸载。</p>

<h2 id="结语">结语</h2>

<p>注意力是 AI 时代最后的稀缺资源——算力在变便宜，模型在变强，执行在变快，唯独人类清醒、连续、高质量的注意力，一天还是只有那么几个小时，而且正在被更多东西争夺。</p>

<p>深度工作在前 AI 时代是效率优势，在 AI 时代更像生存前提：留给人的工作，剩下的基本都是必须深度才做得好的部分——定义问题、设定约束、做判断。浅的部分 AI 都拿走了。</p>

<p>如果想试试，给个最小起步：明天上午挑两小时，关掉所有 AI 对话窗口，只带一个明确的问题进去，比如「下季度这个系统最大的技术风险是什么」，结束时写成一页纸。拿这一页纸去派 Agent 任务，对比一下平时随手发任务的产出质量，自己感受。我赌你回不去了。</p>]]></content><author><name>小黄鸭</name></author><category term="思考" /><category term="思考" /><category term="深度工作" /><category term="注意力" /><category term="AI" /><category term="效率" /><summary type="html"><![CDATA[当 AI 把执行成本压到接近于零，产出的瓶颈从「做得多快」变成「想得多清」。这篇文章讨论一个反直觉的结论：AI 越强，深度工作越值钱。]]></summary></entry><entry><title type="html">AI Coding 落地存量大项目：规范先于工具</title><link href="http://localhost:4000/ai/%E6%8A%80%E6%9C%AF/2026/05/27/AI-Coding%E8%90%BD%E5%9C%B0%E5%AD%98%E9%87%8F%E5%A4%A7%E9%A1%B9%E7%9B%AE-%E8%A7%84%E8%8C%83%E5%85%88%E4%BA%8E%E5%B7%A5%E5%85%B7.html" rel="alternate" type="text/html" title="AI Coding 落地存量大项目：规范先于工具" /><published>2026-05-27T00:00:00+08:00</published><updated>2026-06-13T14:30:53+08:00</updated><id>http://localhost:4000/ai/%E6%8A%80%E6%9C%AF/2026/05/27/AI-Coding%E8%90%BD%E5%9C%B0%E5%AD%98%E9%87%8F%E5%A4%A7%E9%A1%B9%E7%9B%AE-%E8%A7%84%E8%8C%83%E5%85%88%E4%BA%8E%E5%B7%A5%E5%85%B7</id><content type="html" xml:base="http://localhost:4000/ai/%E6%8A%80%E6%9C%AF/2026/05/27/AI-Coding%E8%90%BD%E5%9C%B0%E5%AD%98%E9%87%8F%E5%A4%A7%E9%A1%B9%E7%9B%AE-%E8%A7%84%E8%8C%83%E5%85%88%E4%BA%8E%E5%B7%A5%E5%85%B7.html"><![CDATA[<p>前几天看到一篇文章，讲某团队用 AI 对 31 万行代码做重构的实践。里面有一句话让我想起了自己团队踩的坑：「没有统一规范的约束，不同人用 AI 写出的代码风格各异，系统反而会加速腐化。」这个说法我们亲身验证过，记录一下。</p>

<h2 id="ai-进来之前问题就已经存在了">AI 进来之前，问题就已经存在了</h2>

<p>我们有一个 Java 项目，五年历史，十几万行代码。架构本来是分层的（Controller → Service → Repository），但随着人员流动，各层的边界越来越模糊：有 Controller 直接调 DAO 的，有 Service 里写了大量业务逻辑又有 Service 把业务逻辑推到 DTO 里的，DTO 后缀有人写 <code class="language-plaintext highlighter-rouge">Dto</code>、有人写 <code class="language-plaintext highlighter-rouge">DTO</code>。同一件事有三种做法，没有一种是错的，但放在一起就是一团浆糊。</p>

<p>这个问题在 AI 之前就存在，但可以接受——新人来了，看看旁边的代码照着写，慢慢理解隐性规范，大约三个月能融入。有问题有人 review，偏差不会太大。</p>

<p>AI 进来之后，问题变成了五倍速。</p>

<p>工具链收拢到 Cursor + Claude CLI 之后，大家产出代码的速度快了，但因为每次 AI 对话都是独立的，AI 不知道这个项目的隐性规范，每次都靠「推断」——看见 Mapper 就直接注入，没看过项目规定必须走 Repository 接口；看见字段是 <code class="language-plaintext highlighter-rouge">userId</code> 就用 Long，没看见项目规定金额类字段必须用 BigDecimal。每个人的 AI 推断出来的默认值不同，合代码的时候就是一锅粥。review 成本比以前高了，因为不是偶尔有一两个风格问题，而是每个 PR 都有一批。</p>

<h2 id="工具统一没用规范落地才有用">工具统一没用，规范落地才有用</h2>

<p>第一个反应是统一工具——大家用同一套 IDE、同一套 prompt 模板。换了之后，情况确实稍微好一点，但没有本质改变。因为问题不是工具不同，是 AI 缺少项目的上下文。</p>

<p>真正解决问题的是做了两件事：</p>

<p><strong>第一步：人人对齐。</strong> 开了一次两小时的架构规范讨论，把那些「大家心里知道但没写下来」的规则显式化：字段注入还是构造器注入？哪些工具类用？分层的边界在哪里？异常怎么包装？金额字段用什么类型？这些讨论并不轻松，因为不同历史时期加入的人有不同的理解，要对齐本身就是一次利益博弈——有人用了三年 <code class="language-plaintext highlighter-rouge">@Autowired</code>，不愿意改。但不对齐就没办法往下走，必须这一步先完成。</p>

<p><strong>第二步：人机对齐。</strong> 把对齐后的规范写进 CLAUDE.md 和 Cursor Rules，让 AI 在生成代码前就能加载这些约束。不是写成「尽量使用构造器注入」，而是写成「必须使用构造器注入，不允许 <code class="language-plaintext highlighter-rouge">@Autowired</code> 字段注入，违反此规则的代码 review 时直接打回」。模糊的规则对 AI 和对人都没用。</p>

<p>这两步的顺序很重要。如果人没有对齐就直接去写 AI 规范，写出来的规范反映的是写规范那个人的习惯，不是团队共识。AI 照着执行，其他人依然不认，规范成了摆设。「人人对齐」必须先于「人机对齐」。</p>

<h2 id="claudemd-写什么不写什么">CLAUDE.md 写什么、不写什么</h2>

<p>花了一些时间摸索规范文件的粒度，记几个经验。</p>

<p><strong>不写太泛的原则，只写可验证的规则。</strong> 「代码要清晰易读」这种话对 AI 没意义，它不知道怎么评估。「所有 public 方法必须有 Javadoc，Javadoc 里必须包含参数说明和返回值说明」这种规则是可执行的。遇到模糊规则先不写，等出了具体的坏案例，反过来把规则提炼清楚再写进去。</p>

<p><strong>写决策表，不写判断过程。</strong> 「优先用 X，但如果 Y 情况下用 Z，不过如果还有 W 情况……」这种条件链条对 AI 来说很混乱。改成「场景：分页查询 → 用 X；场景：全量加载 → 用 Z」的决策表形式，执行准确率高很多。</p>

<p><strong>真实代码片段比描述有效。</strong> 在规范里直接贴从项目里截取的三到五行代码，比用文字描述「应该像这样做」效果好得多。注意是从生产代码里截，不是现写示意代码——AI 对「真实风格」和「演示风格」的识别比想象中敏感，示意代码有时候会被 AI 当成另一种新的风格来模仿。</p>

<p><strong>总行数控制在 100~150 行以内。</strong> 超过这个长度，AI 在「中间」的注意力会下降，埋在文件中段的规则遵守率显著变差。如果规范很多，分到各子模块的 CLAUDE.md 里，主文件只写最高频、最关键的那些。</p>

<h2 id="一个月后的数据">一个月后的数据</h2>

<p>做完这两步大约一个月，有几个可以量化的变化。</p>

<p>review 打回次数降了约 60%。以前每个 PR 平均有 4~6 条风格类评论，现在降到 1~2 条，而且多数是「这个 edge case 考虑了吗」这类实质性问题，不是风格问题。</p>

<p>新功能的 AI 代码采纳率从大约 40% 涨到 75%。「采纳率」是我自己定义的指标：AI 生成的代码未经大幅修改就进入最终 PR 的比例。这个数字高不代表 AI 在偷懒，代表 AI 生成的代码方向对了，减少了二次修改。</p>

<p>不好量化但能感受到的：大家对「AI 写出来的代码是不是我们的代码」的接受度提高了。以前合了 AI 生成的代码，总有一种「这不是我们的风格，只是凑合能跑」的感觉；现在因为规范一致，AI 的产出和人工写的代码混在一起，review 的时候不明显区分了。</p>

<h2 id="还没解决的">还没解决的</h2>

<p>有两件事还没有好的解法。</p>

<p>一是规范的维护成本。CLAUDE.md 是个静态文件，项目的规范会演进，有新人加入带来新的约定，有旧的约定被废弃，文件容易腐烂。目前的做法是每季度做一次规范 review，但坦白说这件事很难坚持，往往是出了问题才回头更新。</p>

<p>二是架构层面的约束还是穿不进去。字段类型、命名规范这类表面的约束写进 CLAUDE.md 很有效；但「Service 不能直接访问 DAO」这种架构边界，AI 知道规则但不理解违反规则的系统性后果，在模糊情况下（比如一个临时的 one-off 查询）还是会绕过边界。这类约束真正有效的手段还是代码层面的封装——让违规在编译时就报错，而不只是写在文档里等 review 时发现。</p>

<p>总结一下教训：AI Coding 在存量大项目里落地，工具和模型选型只是入场券，规范工程才是核心工作量。而规范工程最容易踩的坑，是跳过「人人对齐」直接去写 AI 规范——写出来的规范没有权威性，工具执行得再准也没有意义。</p>]]></content><author><name>小黄鸭</name></author><category term="AI" /><category term="技术" /><category term="AI" /><category term="AI Coding" /><category term="工程规范" /><category term="重构" /><category term="团队实践" /><summary type="html"><![CDATA[AI Coding 落地大型存量项目，遇到的最大问题不是模型能力，是规范缺失被 AI 放大了。聊一下我们团队在一个 Java 老项目上的踩坑经历，以及「人人对齐 → 人机对齐」这个顺序为什么重要。]]></summary></entry><entry><title type="html">MySQL 向量检索实践：当数据库遇上 Embedding</title><link href="http://localhost:4000/%E6%8A%80%E6%9C%AF/mysql/2026/05/23/MySQL-%E5%90%91%E9%87%8F%E6%A3%80%E7%B4%A2%E5%AE%9E%E8%B7%B5-%E5%BD%93%E6%95%B0%E6%8D%AE%E5%BA%93%E9%81%87%E4%B8%8AEmbedding.html" rel="alternate" type="text/html" title="MySQL 向量检索实践：当数据库遇上 Embedding" /><published>2026-05-23T00:00:00+08:00</published><updated>2026-06-13T08:13:18+08:00</updated><id>http://localhost:4000/%E6%8A%80%E6%9C%AF/mysql/2026/05/23/MySQL-%E5%90%91%E9%87%8F%E6%A3%80%E7%B4%A2%E5%AE%9E%E8%B7%B5-%E5%BD%93%E6%95%B0%E6%8D%AE%E5%BA%93%E9%81%87%E4%B8%8AEmbedding</id><content type="html" xml:base="http://localhost:4000/%E6%8A%80%E6%9C%AF/mysql/2026/05/23/MySQL-%E5%90%91%E9%87%8F%E6%A3%80%E7%B4%A2%E5%AE%9E%E8%B7%B5-%E5%BD%93%E6%95%B0%E6%8D%AE%E5%BA%93%E9%81%87%E4%B8%8AEmbedding.html"><![CDATA[<p>做 RAG 的团队都绕不开一个选型问题：向量存哪？专用向量库（Milvus、Qdrant）功能强但要多养一套系统；MySQL 9 开始原生支持 <code class="language-plaintext highlighter-rouge">VECTOR</code> 类型后，「就用现有 MySQL」成了一个真实选项。这篇文章实测 MySQL 向量能力的边界，给出选型决策框架。</p>

<h2 id="mysql-的向量能力现状">MySQL 的向量能力现状</h2>

<p>MySQL 9.x 提供了 <code class="language-plaintext highlighter-rouge">VECTOR(N)</code> 列类型和基础的距离函数。建表和写入：</p>

<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">CREATE</span> <span class="k">TABLE</span> <span class="n">doc_chunks</span> <span class="p">(</span>
  <span class="n">id</span> <span class="nb">BIGINT</span> <span class="n">AUTO_INCREMENT</span> <span class="k">PRIMARY</span> <span class="k">KEY</span><span class="p">,</span>
  <span class="n">doc_id</span> <span class="nb">BIGINT</span> <span class="k">NOT</span> <span class="k">NULL</span><span class="p">,</span>
  <span class="n">chunk_text</span> <span class="nb">TEXT</span> <span class="k">NOT</span> <span class="k">NULL</span><span class="p">,</span>
  <span class="n">embedding</span> <span class="n">VECTOR</span><span class="p">(</span><span class="mi">1024</span><span class="p">)</span> <span class="k">NOT</span> <span class="k">NULL</span><span class="p">,</span>
  <span class="n">tenant_id</span> <span class="nb">INT</span> <span class="k">NOT</span> <span class="k">NULL</span><span class="p">,</span>
  <span class="n">created_at</span> <span class="nb">DATETIME</span> <span class="k">DEFAULT</span> <span class="k">CURRENT_TIMESTAMP</span><span class="p">,</span>
  <span class="k">KEY</span> <span class="n">idx_tenant_doc</span> <span class="p">(</span><span class="n">tenant_id</span><span class="p">,</span> <span class="n">doc_id</span><span class="p">)</span>
<span class="p">)</span> <span class="n">ENGINE</span><span class="o">=</span><span class="n">InnoDB</span><span class="p">;</span>

<span class="k">INSERT</span> <span class="k">INTO</span> <span class="n">doc_chunks</span> <span class="p">(</span><span class="n">doc_id</span><span class="p">,</span> <span class="n">chunk_text</span><span class="p">,</span> <span class="n">embedding</span><span class="p">,</span> <span class="n">tenant_id</span><span class="p">)</span>
<span class="k">VALUES</span> <span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="s1">'...'</span><span class="p">,</span> <span class="n">STRING_TO_VECTOR</span><span class="p">(</span><span class="s1">'[0.0231, -0.114, ...]'</span><span class="p">),</span> <span class="mi">42</span><span class="p">);</span>
</code></pre></div></div>

<p>查询用距离函数排序：</p>

<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">SELECT</span> <span class="n">id</span><span class="p">,</span> <span class="n">chunk_text</span><span class="p">,</span>
       <span class="n">DISTANCE</span><span class="p">(</span><span class="n">embedding</span><span class="p">,</span> <span class="n">STRING_TO_VECTOR</span><span class="p">(</span><span class="o">?</span><span class="p">),</span> <span class="s1">'COSINE'</span><span class="p">)</span> <span class="k">AS</span> <span class="n">dist</span>
<span class="k">FROM</span> <span class="n">doc_chunks</span>
<span class="k">WHERE</span> <span class="n">tenant_id</span> <span class="o">=</span> <span class="mi">42</span>
<span class="k">ORDER</span> <span class="k">BY</span> <span class="n">dist</span>
<span class="k">LIMIT</span> <span class="mi">10</span><span class="p">;</span>
</code></pre></div></div>

<p>关键问题来了：<strong>社区版 MySQL 的这条查询是全表暴力扫描</strong>。每一行都要算一次 1024 维余弦距离，没有 ANN（近似最近邻）索引加持。HeatWave 版本有向量索引和加速，但那是云上付费能力。所以社区版 MySQL 做向量检索的真实边界，取决于暴力扫描扛到多大数据量。</p>

<h2 id="实测暴力扫描能扛多少">实测：暴力扫描能扛多少</h2>

<p>测试环境 8C16G，MySQL 9.3，1024 维 float 向量，单表测试 cosine 距离 + LIMIT 10：</p>

<table>
  <thead>
    <tr>
      <th>行数</th>
      <th>无过滤条件</th>
      <th>tenant 过滤后剩 5%</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>10 万</td>
      <td>~180ms</td>
      <td>~15ms</td>
    </tr>
    <tr>
      <td>50 万</td>
      <td>~900ms</td>
      <td>~55ms</td>
    </tr>
    <tr>
      <td>200 万</td>
      <td>~3.8s</td>
      <td>~210ms</td>
    </tr>
  </tbody>
</table>

<p>两个结论。第一，裸扫 50 万行就接近交互式应用的容忍上限了。第二，<strong>带过滤条件的场景表现完全不同</strong>——只要二级索引能先把候选集裁到几万行，距离计算就只发生在小集合上，性能立刻回到可用区间。</p>

<p>这正好戳中专用向量库的软肋：预过滤（pre-filtering）。多租户 SaaS 场景下「只在租户 42 的文档里搜」，很多向量库的 ANN 索引做带过滤的检索反而别扭（filter 越严，HNSW 图遍历越容易断裂，召回率下降）。MySQL 用 B+ 树索引先过滤再暴力算距离，是「精确解」，没有召回损失。</p>

<p><img src="/assets/images/2026/mysql-vector-decision.svg" alt="向量检索选型决策" /></p>

<h2 id="务实的混合架构">务实的混合架构</h2>

<p>基于实测，我在生产中采用的判断标准：</p>

<p><strong>用 MySQL 的场景</strong>：候选集经过业务条件过滤后在 10 万行以内；向量数据和业务数据强关联（要 JOIN 用户表、权限表）；团队没有专人运维新组件。典型如内部知识库、客服工单检索、中小规模 SaaS 的租户内搜索。</p>

<p><strong>上专用向量库的场景</strong>：全局检索千万级以上向量；需要高 QPS（&gt;500）低延迟（&lt;50ms）的纯向量查询;需要标量量化、多向量、稀疏向量等高级能力。</p>

<p>很多团队的真实情况是第一种，却按第二种做了架构，平白多养一个有状态集群，还要解决 MySQL 与向量库之间的数据同步一致性——这往往才是最大的隐性成本。双写丢数据、CDC 链路延迟、删除不同步导致检索出已删内容，每一个都是真实事故来源。</p>

<h2 id="几个落地细节">几个落地细节</h2>

<p><strong>维度越低越好，够用就行。</strong> 1024 维和 256 维在很多业务召回评测上差距不到 2%，但存储和计算差 4 倍。新一代 embedding 模型普遍支持 Matryoshka 降维（截断前 N 维即可用），生产里我用 256 维起步，评测不达标再升。</p>

<p><strong>距离计算下推，不要捞回应用层算。</strong> 见过把 embedding 全部 SELECT 回 Python 再算相似度的，网络传输比计算还慢。距离函数永远在 SQL 里执行。</p>

<p><strong>chunk 表要做冷热分离。</strong> 文档更新会产生大量死 chunk，定期归档，控制参与扫描的行数——既然是暴力扫描，行数就是生命线。</p>

<p><strong>召回之后必须重排。</strong> 向量检索只是粗筛，top 50 召回后用轻量 reranker（如 bge-reranker）精排到 top 5，端到端效果提升远大于把向量库换来换去。RAG 的瓶颈大概率不在检索引擎，而在 chunking 策略和重排。</p>

<h2 id="容易被问到的三个问题">容易被问到的三个问题</h2>

<p><strong>「为什么不用 PostgreSQL + pgvector？」</strong> pgvector 确实更成熟，有 HNSW 索引，社区版就能用，如果你的技术栈本来就是 PG，无脑选它。本文的前提是「存量系统是 MySQL」——为了向量检索把主数据库从 MySQL 迁到 PG，比引入一个向量库的动静还大。选型永远要带着存量约束讨论，脱离存量谈最优解是纸上谈兵。</p>

<p><strong>「暴力扫描的 CPU 占用会不会影响业务查询？」</strong> 会，这是真实风险。距离计算是纯 CPU 密集操作，200 万行一次扫描能把一个核打满几秒。两个缓解手段：向量查询走只读副本，与交易流量物理隔离；用 <code class="language-plaintext highlighter-rouge">MAX_EXECUTION_TIME</code> hint 给向量查询设置超时上限，宁可检索降级也不拖垮副本。</p>

<p><strong>「embedding 模型升级了怎么办？」</strong> 这是所有方案共同的痛：换模型意味着全量重算向量。MySQL 方案在这里反而有点优势——加一列 <code class="language-plaintext highlighter-rouge">embedding_v2 VECTOR(256)</code>，后台任务批量回填，灌完后切换查询列、删旧列，全程不停服，用的都是 DBA 最熟悉的在线变更套路，不需要学习向量库的 collection 迁移机制。设计 schema 时建议直接带上 <code class="language-plaintext highlighter-rouge">model_version</code> 字段，给未来的自己留路。</p>

<h2 id="一段-python-接入示例">一段 Python 接入示例</h2>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">aiomysql</span><span class="p">,</span> <span class="n">numpy</span> <span class="k">as</span> <span class="n">np</span>

<span class="k">async</span> <span class="k">def</span> <span class="nf">search</span><span class="p">(</span><span class="n">pool</span><span class="p">,</span> <span class="n">tenant_id</span><span class="p">:</span> <span class="nb">int</span><span class="p">,</span> <span class="n">query_vec</span><span class="p">:</span> <span class="n">np</span><span class="p">.</span><span class="n">ndarray</span><span class="p">,</span> <span class="n">k</span><span class="p">:</span> <span class="nb">int</span> <span class="o">=</span> <span class="mi">10</span><span class="p">):</span>
    <span class="n">vec_str</span> <span class="o">=</span> <span class="sh">"</span><span class="s">[</span><span class="sh">"</span> <span class="o">+</span> <span class="sh">"</span><span class="s">,</span><span class="sh">"</span><span class="p">.</span><span class="nf">join</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="si">{</span><span class="n">x</span><span class="si">:</span><span class="p">.</span><span class="mi">6</span><span class="n">f</span><span class="si">}</span><span class="sh">"</span> <span class="k">for</span> <span class="n">x</span> <span class="ow">in</span> <span class="n">query_vec</span><span class="p">)</span> <span class="o">+</span> <span class="sh">"</span><span class="s">]</span><span class="sh">"</span>
    <span class="k">async</span> <span class="k">with</span> <span class="n">pool</span><span class="p">.</span><span class="nf">acquire</span><span class="p">()</span> <span class="k">as</span> <span class="n">conn</span><span class="p">:</span>
        <span class="k">async</span> <span class="k">with</span> <span class="n">conn</span><span class="p">.</span><span class="nf">cursor</span><span class="p">(</span><span class="n">aiomysql</span><span class="p">.</span><span class="n">DictCursor</span><span class="p">)</span> <span class="k">as</span> <span class="n">cur</span><span class="p">:</span>
            <span class="k">await</span> <span class="n">cur</span><span class="p">.</span><span class="nf">execute</span><span class="p">(</span><span class="sh">"""</span><span class="s">
                SELECT id, chunk_text,
                       DISTANCE(embedding, STRING_TO_VECTOR(%s), </span><span class="sh">'</span><span class="s">COSINE</span><span class="sh">'</span><span class="s">) AS dist
                FROM doc_chunks
                WHERE tenant_id = %s
                ORDER BY dist LIMIT %s
            </span><span class="sh">"""</span><span class="p">,</span> <span class="p">(</span><span class="n">vec_str</span><span class="p">,</span> <span class="n">tenant_id</span><span class="p">,</span> <span class="n">k</span><span class="p">))</span>
            <span class="k">return</span> <span class="k">await</span> <span class="n">cur</span><span class="p">.</span><span class="nf">fetchall</span><span class="p">()</span>
</code></pre></div></div>

<p>注意向量序列化成字符串传参的开销在高 QPS 下不可忽略，可以预编译语句并复用连接；批量写入时一个事务攒 500~1000 行提交，吞吐能差一个数量级。</p>

<h2 id="结语">结语</h2>

<p>我们最后的落地方案就是最朴素的那种：MySQL 存向量，只读副本跑检索，候选集靠租户过滤压到万级以内，top 50 召回后过一遍 reranker。上线三个月，没有为它新增任何组件，也没有为它起过一次夜。</p>

<p>MySQL 的向量能力不是用来对标 Milvus 的，它的价值是让一大批中等规模、强业务关联的场景不用引入新组件。等哪天数据量真长到这个方案撑不住，再迁也来得及——到那时业务也验证过了，迁移的钱花得不冤。</p>]]></content><author><name>小黄鸭</name></author><category term="技术" /><category term="MySQL" /><category term="MySQL" /><category term="向量检索" /><category term="Embedding" /><category term="RAG" /><category term="架构设计" /><summary type="html"><![CDATA[MySQL 9 引入 VECTOR 类型后，「要不要为 RAG 单独养一个向量数据库」有了新答案。本文实测 MySQL 向量能力的边界，并给出一套务实的选型决策框架。]]></summary></entry><entry><title type="html">Go pprof 性能剖析实战：从火焰图到根因</title><link href="http://localhost:4000/%E6%8A%80%E6%9C%AF/golang/2026/05/17/Go-pprof%E6%80%A7%E8%83%BD%E5%89%96%E6%9E%90%E5%AE%9E%E6%88%98-%E4%BB%8E%E7%81%AB%E7%84%B0%E5%9B%BE%E5%88%B0%E6%A0%B9%E5%9B%A0.html" rel="alternate" type="text/html" title="Go pprof 性能剖析实战：从火焰图到根因" /><published>2026-05-17T00:00:00+08:00</published><updated>2026-06-13T08:13:11+08:00</updated><id>http://localhost:4000/%E6%8A%80%E6%9C%AF/golang/2026/05/17/Go-pprof%E6%80%A7%E8%83%BD%E5%89%96%E6%9E%90%E5%AE%9E%E6%88%98-%E4%BB%8E%E7%81%AB%E7%84%B0%E5%9B%BE%E5%88%B0%E6%A0%B9%E5%9B%A0</id><content type="html" xml:base="http://localhost:4000/%E6%8A%80%E6%9C%AF/golang/2026/05/17/Go-pprof%E6%80%A7%E8%83%BD%E5%89%96%E6%9E%90%E5%AE%9E%E6%88%98-%E4%BB%8E%E7%81%AB%E7%84%B0%E5%9B%BE%E5%88%B0%E6%A0%B9%E5%9B%A0.html"><![CDATA[<p>线上 Go 服务出问题，无非三类：CPU 打满、内存暴涨、goroutine 泄漏。pprof 是 Go 内置的剖析工具，但很多人只会 <code class="language-plaintext highlighter-rouge">go tool pprof</code> 然后对着输出发呆。这篇文章讲一套完整的方法论：怎么采样、怎么读火焰图、怎么从图定位到代码行，最后给三个真实案例。</p>

<h2 id="先把采样端配好">先把采样端配好</h2>

<p>生产服务直接挂 HTTP 端点，一行导入：</p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">import</span> <span class="n">_</span> <span class="s">"net/http/pprof"</span>

<span class="k">func</span> <span class="n">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="k">go</span> <span class="k">func</span><span class="p">()</span> <span class="p">{</span>
        <span class="c">// 单独端口，不要暴露公网</span>
        <span class="n">log</span><span class="o">.</span><span class="n">Println</span><span class="p">(</span><span class="n">http</span><span class="o">.</span><span class="n">ListenAndServe</span><span class="p">(</span><span class="s">"127.0.0.1:6060"</span><span class="p">,</span> <span class="no">nil</span><span class="p">))</span>
    <span class="p">}()</span>
    <span class="c">// ... 业务逻辑</span>
<span class="p">}</span>
</code></pre></div></div>

<p>这会注册一组路由到 <code class="language-plaintext highlighter-rouge">/debug/pprof/</code> 下。最常用的四个：</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># CPU：采样 30 秒内的 CPU 时间分布</span>
go tool pprof http://127.0.0.1:6060/debug/pprof/profile?seconds<span class="o">=</span>30

<span class="c"># 堆内存：当前存活对象的分配情况</span>
go tool pprof http://127.0.0.1:6060/debug/pprof/heap

<span class="c"># goroutine：所有协程的调用栈</span>
go tool pprof http://127.0.0.1:6060/debug/pprof/goroutine

<span class="c"># 阻塞与锁竞争（需要先 SetBlockProfileRate / SetMutexProfileFraction）</span>
go tool pprof http://127.0.0.1:6060/debug/pprof/mutex
</code></pre></div></div>

<p>两个容易忽略的点：CPU profile 是「采样期间」的快照，问题没复现时采了也白采，要在故障发生时采；heap profile 默认展示 <code class="language-plaintext highlighter-rouge">inuse_space</code>（存活内存），排查「分配太频繁导致 GC 压力大」要切到 <code class="language-plaintext highlighter-rouge">alloc_space</code> 视角：<code class="language-plaintext highlighter-rouge">go tool pprof -sample_index=alloc_space</code>。</p>

<h2 id="火焰图怎么读">火焰图怎么读</h2>

<p><code class="language-plaintext highlighter-rouge">go tool pprof -http=:8080 profile.pb.gz</code> 打开 Web UI，切到 Flame Graph 视图。读图只需要记三条规则：</p>

<p><strong>横轴是占比，不是时间顺序。</strong> 一个函数的宽度代表它（含子调用）占用的 CPU 采样比例。找最宽的塔。</p>

<p><strong>纵轴是调用栈，从下往上。</strong> 底部是入口（如 <code class="language-plaintext highlighter-rouge">main.main</code> 或某个 goroutine 起点），越往上越深。</p>

<p><strong>找「平顶」。</strong> 顶部又宽又平的帧意味着 CPU 真正消耗在这个函数自身（self time），而不是它的子调用——这就是优化目标。</p>

<p><img src="/assets/images/2026/pprof-flamegraph-guide.svg" alt="火焰图判读示意" /></p>

<p>图形看不清时回到命令行，<code class="language-plaintext highlighter-rouge">top</code> 看 self 时间排序，<code class="language-plaintext highlighter-rouge">list 函数名</code> 直接看到每一行代码的耗时标注——这是 pprof 最被低估的命令。</p>

<h2 id="案例一cpu-打满凶手是-json">案例一：CPU 打满，凶手是 JSON</h2>

<p>某服务 QPS 没变，CPU 却从 40% 涨到 90%。采 CPU profile，火焰图上 <code class="language-plaintext highlighter-rouge">encoding/json.Marshal</code> 一族占了 58%，平顶集中在 <code class="language-plaintext highlighter-rouge">reflect.Value</code> 相关帧。</p>

<p><code class="language-plaintext highlighter-rouge">list</code> 定位到一个监控上报函数：每次请求都把完整上下文结构体（嵌套七层）序列化成 JSON 打日志。一周前有人往结构体里加了个大 map。</p>

<p>修复分三层。短期：日志降采样，1% 抽样上报。中期：这个路径换成手写的 <code class="language-plaintext highlighter-rouge">MarshalJSON</code> 只输出关键字段。长期：在 CI 里加 benchmark 守门：</p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">func</span> <span class="n">BenchmarkReportMarshal</span><span class="p">(</span><span class="n">b</span> <span class="o">*</span><span class="n">testing</span><span class="o">.</span><span class="n">B</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">ctx</span> <span class="o">:=</span> <span class="n">buildFullContext</span><span class="p">()</span>
    <span class="n">b</span><span class="o">.</span><span class="n">ReportAllocs</span><span class="p">()</span>
    <span class="k">for</span> <span class="n">i</span> <span class="o">:=</span> <span class="m">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">b</span><span class="o">.</span><span class="n">N</span><span class="p">;</span> <span class="n">i</span><span class="o">++</span> <span class="p">{</span>
        <span class="n">_</span><span class="p">,</span> <span class="n">_</span> <span class="o">=</span> <span class="n">json</span><span class="o">.</span><span class="n">Marshal</span><span class="p">(</span><span class="n">ctx</span><span class="p">)</span>
    <span class="p">}</span>
<span class="p">}</span>
<span class="c">// 配合 benchstat 对比 PR 前后，劣化超 20% 报警</span>
</code></pre></div></div>

<p>CPU 回到 45%。教训：反射型序列化在热路径上就是定时炸弹，<code class="language-plaintext highlighter-rouge">json.Marshal</code> 出现在火焰图前排时先怀疑它。</p>

<h2 id="案例二内存缓慢上涨不是泄漏是逃逸">案例二：内存缓慢上涨，不是泄漏是「逃逸」</h2>

<p>另一个服务内存每天涨 300MB，重启后归零。heap profile 的 <code class="language-plaintext highlighter-rouge">inuse_space</code> 里 <code class="language-plaintext highlighter-rouge">bytes.growSlice</code> 占大头，调用方是一个缓冲池的 <code class="language-plaintext highlighter-rouge">Get</code>。</p>

<p>看代码发现这是个经典错误：<code class="language-plaintext highlighter-rouge">sync.Pool</code> 里放 <code class="language-plaintext highlighter-rouge">*bytes.Buffer</code>，但有个慢路径会把 buffer 扩到几十 MB，用完照样放回池里。池子越养越肥。</p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">func</span> <span class="n">putBuffer</span><span class="p">(</span><span class="n">b</span> <span class="o">*</span><span class="n">bytes</span><span class="o">.</span><span class="n">Buffer</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if</span> <span class="n">b</span><span class="o">.</span><span class="n">Cap</span><span class="p">()</span> <span class="o">&gt;</span> <span class="m">1</span><span class="o">&lt;&lt;</span><span class="m">20</span> <span class="p">{</span> <span class="c">// 超过 1MB 的不回池，让 GC 收走</span>
        <span class="k">return</span>
    <span class="p">}</span>
    <span class="n">b</span><span class="o">.</span><span class="n">Reset</span><span class="p">()</span>
    <span class="n">bufPool</span><span class="o">.</span><span class="n">Put</span><span class="p">(</span><span class="n">b</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div>

<p>顺手用逃逸分析确认了另一个热点函数的分配来源：</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>go build <span class="nt">-gcflags</span><span class="o">=</span><span class="s1">'-m -m'</span> ./pkg/codec/ 2&gt;&amp;1 | <span class="nb">grep </span>escape
<span class="c"># ./codec.go:42:13: buf escapes to heap: 传给了 interface 参数</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">interface{}</code> 参数、闭包捕获、返回局部 slice 指针，是逃逸三大常客。内存问题不全是泄漏，「该死的对象死不掉」和「不该堆分配的上了堆」同样常见，前者看 inuse，后者看 alloc。</p>

<h2 id="案例三goroutine-泄漏每小时多一千个">案例三：goroutine 泄漏，每小时多一千个</h2>

<p>监控显示 goroutine 数量线性增长。直接看文本版快照，按栈聚合：</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="nt">-s</span> <span class="s1">'http://127.0.0.1:6060/debug/pprof/goroutine?debug=1'</span> | <span class="nb">head</span> <span class="nt">-30</span>
<span class="c"># 1024 @ 0x43a1c5 0x44f9e7 ...</span>
<span class="c"># #  chan send, 47 minutes</span>
<span class="c"># #  github.com/xx/svc/worker.process+0x1a4</span>
</code></pre></div></div>

<p>一千多个 goroutine 卡在同一个 <code class="language-plaintext highlighter-rouge">chan send</code>，已经卡了 47 分钟。读 <code class="language-plaintext highlighter-rouge">worker.process</code>：它往一个结果 channel 发送数据，而接收方在超时后直接 return 了，没人收，发送方永久阻塞。</p>

<p>修复原则就一条——发送方永远要有退路：</p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">select</span> <span class="p">{</span>
<span class="k">case</span> <span class="n">resultCh</span> <span class="o">&lt;-</span> <span class="n">res</span><span class="o">:</span>
<span class="k">case</span> <span class="o">&lt;-</span><span class="n">ctx</span><span class="o">.</span><span class="n">Done</span><span class="p">()</span><span class="o">:</span> <span class="c">// 接收方超时离场后，发送方从这里退出</span>
    <span class="k">return</span> <span class="n">ctx</span><span class="o">.</span><span class="n">Err</span><span class="p">()</span>
<span class="p">}</span>
</code></pre></div></div>

<p>更系统的防御是给所有 worker 类 goroutine 强制传 context，并在集成测试里用 <code class="language-plaintext highlighter-rouge">goleak</code> 检查：</p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">func</span> <span class="n">TestMain</span><span class="p">(</span><span class="n">m</span> <span class="o">*</span><span class="n">testing</span><span class="o">.</span><span class="n">M</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">goleak</span><span class="o">.</span><span class="n">VerifyTestMain</span><span class="p">(</span><span class="n">m</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div>

<h2 id="持续剖析从救火到巡检">持续剖析：从救火到巡检</h2>

<p>以上都是「出了事再采」的被动模式。更进一步是持续剖析（Continuous Profiling）：agent 常驻采样（开销约 1~3% CPU），profile 数据带时间戳入库，出问题时直接对比「故障时段 vs 正常时段」的差分火焰图，连复现都省了。Pyroscope、Parca 都是现成方案，Go 的原生支持也很好。我把它当作 APM 三件套（metrics、trace、log）之后的第四件，中型以上的服务建议默认开启——它把「能不能采到现场」这个最大的不确定性直接消灭了。</p>

<h2 id="方法论总结">方法论总结</h2>

<p>三个案例背后是同一套流程：先用监控确定问题类别（CPU/内存/goroutine），选对 profile 类型和采样视角；火焰图找最宽的塔和平顶帧，<code class="language-plaintext highlighter-rouge">list</code> 下钻到代码行；修复后用 benchmark + benchstat 量化收益，并把检查固化进 CI。</p>

<p>pprof 还有不少本文没碰的能力，比如配合 <code class="language-plaintext highlighter-rouge">runtime/trace</code> 分析调度延迟和 GC 停顿，不过那是另一个话题了。日常的线上问题，上面这套流程够用八成。剩下两成，通常卡在「采的时候问题没发生」——所以再啰嗦一遍：持续剖析早点上。</p>]]></content><author><name>小黄鸭</name></author><category term="技术" /><category term="Golang" /><category term="Golang" /><category term="pprof" /><category term="性能优化" /><category term="火焰图" /><category term="工程实践" /><summary type="html"><![CDATA[CPU 打满、内存暴涨、goroutine 泄漏——三类最常见的 Go 线上问题，用 pprof 一套方法论打穿：怎么采、怎么看、怎么定位到代码行。]]></summary></entry><entry><title type="html">异步 Agent：把任务扔进后台之后</title><link href="http://localhost:4000/ai/%E6%8A%80%E6%9C%AF/2026/05/14/%E5%BC%82%E6%AD%A5Agent-%E6%8A%8A%E4%BB%BB%E5%8A%A1%E6%89%94%E8%BF%9B%E5%90%8E%E5%8F%B0%E4%B9%8B%E5%90%8E.html" rel="alternate" type="text/html" title="异步 Agent：把任务扔进后台之后" /><published>2026-05-14T00:00:00+08:00</published><updated>2026-06-13T14:30:50+08:00</updated><id>http://localhost:4000/ai/%E6%8A%80%E6%9C%AF/2026/05/14/%E5%BC%82%E6%AD%A5Agent-%E6%8A%8A%E4%BB%BB%E5%8A%A1%E6%89%94%E8%BF%9B%E5%90%8E%E5%8F%B0%E4%B9%8B%E5%90%8E</id><content type="html" xml:base="http://localhost:4000/ai/%E6%8A%80%E6%9C%AF/2026/05/14/%E5%BC%82%E6%AD%A5Agent-%E6%8A%8A%E4%BB%BB%E5%8A%A1%E6%89%94%E8%BF%9B%E5%90%8E%E5%8F%B0%E4%B9%8B%E5%90%8E.html"><![CDATA[<p>今年明显感受到一个变化：越来越多的任务开始用「扔进后台跑」的方式处理，而不是坐在终端前盯着模型一步步走。Cognition 的 Walden Yan 在一次访谈里提到，Devin 自己仓库里 AI 贡献的 commit 占比从一月的 16% 涨到三月的 80%，工程团队人数只增加了 10%。这个数字我觉得代表了行业正在发生的变化方向。</p>

<p>但背景 Agent 真的能用，是最近几个月的事。更早之前，模型能力还撑不住「给任务、等结果」这种交互方式，中间需要人不断扶着走。Opus 4.5 之后，一些任务开始可以真正放手——写好 spec 扔进去，几小时后拿 PR。这对工作方式影响挺大的，但随之带来的工程问题也不少。</p>

<h2 id="任务描述是最大的坑">任务描述是最大的坑</h2>

<p>背景 Agent 和同步 Agent 最大的操作差异，在于你没机会在中途纠偏。同步模式下，Agent 走偏了，你看到了可以立刻打断重来；异步模式下，你发出任务去做别的事，回来发现它跑了两小时、方向整个错了。</p>

<p>所以任务描述的质量决定了异步 Agent 能不能用。我现在发一个异步任务之前，会强制给自己写三件事：</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>目标：要解决什么问题（不是「要做什么」，是「解决什么」）
约束：不能动什么、不能假设什么
验收标准：怎么判断任务完成了
</code></pre></div></div>

<p>这三件事写不清楚就不发，因为 Agent 填充模糊空间的方式通常不是你想要的。最常见的问题是「目标」写成了「行动」——「重构 user.go 里的数据库查询」和「降低 user.go 里数据库查询的延迟 P99」，同样的起点，Agent 的行为路径可以差很远。</p>

<p>约束那栏经常被忽略。我有一次让 Agent 「优化这个模块的性能」，没写约束，它把数据库查询从 ORM 改成了手写 SQL，顺手删掉了几个它觉得「冗余」的封装层。技术上没错，但打破了团队的编码规范，review 打回来重来。后来约束那栏开始写：「不改变代码结构，只在现有框架内优化；不删除任何现有函数，只能修改实现」。</p>

<p>验收标准最容易写成「代码跑通就行」，但这往往不够。现在我会加「运行 <code class="language-plaintext highlighter-rouge">just test</code> 全绿」「新增函数必须有测试覆盖」「不引入新的外部依赖」这类可以机器验证的标准，让 Agent 自己在提交前跑一遍检查。</p>

<h2 id="进度跟踪是个没有标准答案的问题">进度跟踪是个没有标准答案的问题</h2>

<p>任务扔进后台之后，你怎么知道它在干什么、有没有卡住？</p>

<p>目前我试过几种方式，各有缺陷。</p>

<p><strong>定期回来看输出</strong>：最简单，但 Agent 的输出是流式的，你回来看到的是一堆工具调用记录，读起来费劲，很难判断进展是否正常。</p>

<p><strong>让 Agent 写进度日志</strong>：在任务描述里要求 Agent 每完成一个阶段就向 <code class="language-plaintext highlighter-rouge">progress.md</code> 写一行记录。好处是可以 tail -f，坏处是 Agent 有时候忘写，有时候写了很多废话，真正有用的进展信息反而不清楚。</p>

<p><strong>关键检查点 + 等待确认</strong>：把任务拆成几个阶段，每个阶段结束要求 Agent 等待人工 <code class="language-plaintext highlighter-rouge">approve</code> 再继续。这是最可控的，但破坏了「不用盯着它跑」的初衷，变成了另一种形式的同步。</p>

<p>我现在用的是折中方案：非关键任务（内容生成、测试补充）纯异步，不看进度，看结果；关键任务（涉及数据库变更、影响线上服务的重构）强制设检查点，至少在「开始写代码之前」和「准备提交之前」各要求一次人工确认。</p>

<h2 id="多人协作slack-session-的问题">多人协作：Slack session 的问题</h2>

<p>这是一个大家不太讲但实际很痛的问题。</p>

<p>团队用 Agent 的常见模式是：有人在自己的 Slack 里启动一个 Claude session，发任务，等结果。但这个 session 绑定在发起人的身份和对话历史里，其他人如果想接手这个任务——看看进展、追加需求、让 Agent 继续——基本上做不到，只能靠复制粘贴上下文重开一个新 session。</p>

<p>这是一个真实存在的协作摩擦。背景 Agent 的价值有很大一部分在于「不是一个人的专属工具」——PM 可以发起需求，工程师可以接手执行，另一个工程师可以做 review——但如果每次任务都绑定在发起人的私人 session 里，这个价值就打了折。</p>

<p>短期解法是把任务上下文外化：任务描述写在任务管理系统里（Jira/Linear），Agent 的产出存到仓库里，关键决策写到 <code class="language-plaintext highlighter-rouge">task_notes.md</code> 里，session 本身只是执行容器，不存储关键信息。这样换人接手时，新人拿到的材料是完整的。</p>

<p>长期来看这可能是工具层要解决的问题。但现在工具还没到那一步，只能靠工作习惯来补。</p>

<h2 id="背景-agent-改变了什么">背景 Agent 改变了什么</h2>

<p>跑了几个月异步任务之后，工作节奏确实变了，但变化的方向不是「省了很多时间」，而是「深度和等待的比例变了」。</p>

<p>以前一天里大部分时间是持续的、中等深度的工作——写代码、调 bug、看文档。现在变成了几个短的深度爆发（写 spec、验收 PR、做判断）加上大段的「后台等待」时间。如果等待时间用来做另一件深度工作，整体产出是提升的；如果等待时间消耗在切换和碎片处理上，感觉很忙但实际上不比以前高效。</p>

<p>这个变化对工作安排的要求提高了。以前「坐下来打开编辑器就能干活」，现在需要想清楚「这段时间用来做什么、哪些任务可以并行放进后台」。它不会自动让你更高效，但如果你有意识地设计自己的任务流，它的杠杆效应是真实的。</p>

<p>Cognition 那篇访谈里还有一句话我印象深刻：背景 Agent 落地的关键不再只是模型能力，而是围绕模型的整套工程系统。这句话在我自己折腾了半年之后，觉得比刚看到时更对。</p>]]></content><author><name>小黄鸭</name></author><category term="AI" /><category term="技术" /><category term="AI" /><category term="Agent" /><category term="异步" /><category term="工程实践" /><category term="background agent" /><summary type="html"><![CDATA[背景 Agent 今年开始真正可用。但「把任务扔进后台」这件事，工程上的问题远比看起来多：任务怎么描述、进度怎么跟踪、失败了怎么办、多人协作怎么共享上下文。记录一些实际经历。]]></summary></entry><entry><title type="html">MCP 协议深入：AI 应用的 USB-C 接口</title><link href="http://localhost:4000/ai/%E6%8A%80%E6%9C%AF/2026/05/09/MCP%E5%8D%8F%E8%AE%AE%E6%B7%B1%E5%85%A5-AI%E5%BA%94%E7%94%A8%E7%9A%84USB-C%E6%8E%A5%E5%8F%A3.html" rel="alternate" type="text/html" title="MCP 协议深入：AI 应用的 USB-C 接口" /><published>2026-05-09T00:00:00+08:00</published><updated>2026-06-13T08:13:05+08:00</updated><id>http://localhost:4000/ai/%E6%8A%80%E6%9C%AF/2026/05/09/MCP%E5%8D%8F%E8%AE%AE%E6%B7%B1%E5%85%A5-AI%E5%BA%94%E7%94%A8%E7%9A%84USB-C%E6%8E%A5%E5%8F%A3</id><content type="html" xml:base="http://localhost:4000/ai/%E6%8A%80%E6%9C%AF/2026/05/09/MCP%E5%8D%8F%E8%AE%AE%E6%B7%B1%E5%85%A5-AI%E5%BA%94%E7%94%A8%E7%9A%84USB-C%E6%8E%A5%E5%8F%A3.html"><![CDATA[<p>2024 年 11 月 Anthropic 开源 MCP（Model Context Protocol）时，很多人以为这只是又一个工具调用的封装。一年半过去，OpenAI、Google 先后宣布兼容，MCP 事实上成了 AI 应用接入外部世界的标准接口。这篇文章不讲新闻，讲协议本身：它解决了什么问题、怎么解决的、生产环境里有哪些坑。</p>

<h2 id="为什么需要-mcpnm-问题">为什么需要 MCP：N×M 问题</h2>

<p>在 MCP 之前，每个 AI 应用接入每个数据源都要写一遍胶水代码。你有 N 个 AI 客户端（Claude、Cursor、自研 Agent），M 个数据源（数据库、Slack、内部 API），就要维护 N×M 套集成。每套集成都有自己的认证方式、错误处理、数据格式。</p>

<p>这和 USB 出现之前的外设市场一模一样：每台电脑、每个打印机厂商都有自己的接口。MCP 做的事情就是定义一个统一插口——客户端只要实现一次 MCP Client，就能接入所有 MCP Server；数据源只要实现一次 MCP Server，就能被所有支持 MCP 的应用使用。N×M 变成 N+M。</p>

<p><img src="/assets/images/2026/mcp-architecture.svg" alt="MCP 架构示意" /></p>

<h2 id="协议分层三个核心原语">协议分层：三个核心原语</h2>

<p>MCP 基于 JSON-RPC 2.0，传输层支持 stdio（本地进程）和 Streamable HTTP（远程服务）。协议层定义了三个核心原语，理解它们的区别是用好 MCP 的关键：</p>

<p><strong>Tools（工具）</strong>：模型主动调用的函数。由模型决定什么时候调、传什么参数。比如 <code class="language-plaintext highlighter-rouge">query_database</code>、<code class="language-plaintext highlighter-rouge">send_message</code>。这是大家最熟悉的部分，对应传统的 function calling。</p>

<p><strong>Resources（资源）</strong>：应用控制的上下文数据。注意是「应用控制」而不是「模型控制」——由宿主应用决定把哪些资源注入上下文。比如一个文件的内容、一条数据库记录。Resources 是只读的，有 URI 标识，支持订阅变更。</p>

<p><strong>Prompts（提示模板）</strong>：用户控制的交互模板。用户在 UI 里显式选择触发，比如斜杠命令。Server 可以暴露参数化的提示模板，把领域知识打包给用户复用。</p>

<p>三者的控制权分别在模型、应用、用户手里。很多人把所有东西都塞进 Tools，结果模型在一堆本该是静态上下文的数据上反复做无谓的工具调用，既慢又贵。正确的做法是：动态操作用 Tools，静态上下文用 Resources，固定工作流用 Prompts。</p>

<h2 id="动手写一个-mcp-server">动手写一个 MCP Server</h2>

<p>用官方 Python SDK 写一个查询 MySQL 慢日志的 Server，不到 60 行：</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">mcp.server.fastmcp</span> <span class="kn">import</span> <span class="n">FastMCP</span>
<span class="kn">import</span> <span class="n">aiomysql</span>

<span class="n">mcp</span> <span class="o">=</span> <span class="nc">FastMCP</span><span class="p">(</span><span class="sh">"</span><span class="s">mysql-slowlog</span><span class="sh">"</span><span class="p">)</span>

<span class="nd">@mcp.tool</span><span class="p">()</span>
<span class="k">async</span> <span class="k">def</span> <span class="nf">top_slow_queries</span><span class="p">(</span><span class="n">limit</span><span class="p">:</span> <span class="nb">int</span> <span class="o">=</span> <span class="mi">10</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">查询执行时间最长的慢 SQL，按平均耗时降序</span><span class="sh">"""</span>
    <span class="n">pool</span> <span class="o">=</span> <span class="k">await</span> <span class="nf">get_pool</span><span class="p">()</span>
    <span class="k">async</span> <span class="k">with</span> <span class="n">pool</span><span class="p">.</span><span class="nf">acquire</span><span class="p">()</span> <span class="k">as</span> <span class="n">conn</span><span class="p">:</span>
        <span class="k">async</span> <span class="k">with</span> <span class="n">conn</span><span class="p">.</span><span class="nf">cursor</span><span class="p">(</span><span class="n">aiomysql</span><span class="p">.</span><span class="n">DictCursor</span><span class="p">)</span> <span class="k">as</span> <span class="n">cur</span><span class="p">:</span>
            <span class="k">await</span> <span class="n">cur</span><span class="p">.</span><span class="nf">execute</span><span class="p">(</span><span class="sh">"""</span><span class="s">
                SELECT schema_name, digest_text,
                       count_star AS calls,
                       avg_timer_wait/1e12 AS avg_sec
                FROM performance_schema.events_statements_summary_by_digest
                ORDER BY avg_timer_wait DESC LIMIT %s
            </span><span class="sh">"""</span><span class="p">,</span> <span class="p">(</span><span class="n">limit</span><span class="p">,))</span>
            <span class="n">rows</span> <span class="o">=</span> <span class="k">await</span> <span class="n">cur</span><span class="p">.</span><span class="nf">fetchall</span><span class="p">()</span>
    <span class="k">return</span> <span class="sh">"</span><span class="se">\n</span><span class="sh">"</span><span class="p">.</span><span class="nf">join</span><span class="p">(</span>
        <span class="sa">f</span><span class="sh">"</span><span class="s">[</span><span class="si">{</span><span class="n">r</span><span class="p">[</span><span class="sh">'</span><span class="s">avg_sec</span><span class="sh">'</span><span class="p">]</span><span class="si">:</span><span class="p">.</span><span class="mi">2</span><span class="n">f</span><span class="si">}</span><span class="s">s x</span><span class="si">{</span><span class="n">r</span><span class="p">[</span><span class="sh">'</span><span class="s">calls</span><span class="sh">'</span><span class="p">]</span><span class="si">}</span><span class="s">] </span><span class="si">{</span><span class="n">r</span><span class="p">[</span><span class="sh">'</span><span class="s">digest_text</span><span class="sh">'</span><span class="p">][</span><span class="si">:</span><span class="mi">120</span><span class="p">]</span><span class="si">}</span><span class="sh">"</span>
        <span class="k">for</span> <span class="n">r</span> <span class="ow">in</span> <span class="n">rows</span>
    <span class="p">)</span>

<span class="nd">@mcp.resource</span><span class="p">(</span><span class="sh">"</span><span class="s">schema://{table}</span><span class="sh">"</span><span class="p">)</span>
<span class="k">async</span> <span class="k">def</span> <span class="nf">table_schema</span><span class="p">(</span><span class="n">table</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">暴露表结构作为只读资源</span><span class="sh">"""</span>
    <span class="n">pool</span> <span class="o">=</span> <span class="k">await</span> <span class="nf">get_pool</span><span class="p">()</span>
    <span class="k">async</span> <span class="k">with</span> <span class="n">pool</span><span class="p">.</span><span class="nf">acquire</span><span class="p">()</span> <span class="k">as</span> <span class="n">conn</span><span class="p">:</span>
        <span class="k">async</span> <span class="k">with</span> <span class="n">conn</span><span class="p">.</span><span class="nf">cursor</span><span class="p">()</span> <span class="k">as</span> <span class="n">cur</span><span class="p">:</span>
            <span class="k">await</span> <span class="n">cur</span><span class="p">.</span><span class="nf">execute</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">SHOW CREATE TABLE </span><span class="si">{</span><span class="nf">validate</span><span class="p">(</span><span class="n">table</span><span class="p">)</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
            <span class="nf">return </span><span class="p">(</span><span class="k">await</span> <span class="n">cur</span><span class="p">.</span><span class="nf">fetchone</span><span class="p">())[</span><span class="mi">1</span><span class="p">]</span>

<span class="k">if</span> <span class="n">__name__</span> <span class="o">==</span> <span class="sh">"</span><span class="s">__main__</span><span class="sh">"</span><span class="p">:</span>
    <span class="n">mcp</span><span class="p">.</span><span class="nf">run</span><span class="p">(</span><span class="n">transport</span><span class="o">=</span><span class="sh">"</span><span class="s">stdio</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div>

<p>客户端配置（以 Claude Desktop 为例）：</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"mcpServers"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"mysql-slowlog"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
      </span><span class="nl">"command"</span><span class="p">:</span><span class="w"> </span><span class="s2">"python"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"args"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"/opt/mcp/slowlog_server.py"</span><span class="p">],</span><span class="w">
      </span><span class="nl">"env"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="nl">"MYSQL_DSN"</span><span class="p">:</span><span class="w"> </span><span class="s2">"mysql://readonly@db:3306/perf"</span><span class="w"> </span><span class="p">}</span><span class="w">
    </span><span class="p">}</span><span class="w">
  </span><span class="p">}</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>注意两个细节：工具的 docstring 会直接成为模型看到的工具描述，写得越精确，模型的调用准确率越高；<code class="language-plaintext highlighter-rouge">table_schema</code> 用了 Resource 而不是 Tool，因为表结构是静态上下文，不需要模型反复决策。</p>

<h2 id="生产环境的四个坑">生产环境的四个坑</h2>

<p><strong>坑一：工具数量爆炸。</strong> 接入五六个 MCP Server 后，模型面前可能摆着上百个工具。工具描述全部进入上下文，token 成本飙升不说，模型的选择准确率会显著下降。我的经验阈值是 40 个工具以内；超过就要做工具的动态裁剪——按当前任务语义检索相关工具，而不是全量注入。</p>

<p><strong>坑二：把 MCP Server 当 API 网关用。</strong> 有团队把内部 200 个 REST 接口一比一翻译成 MCP 工具，结果模型完全用不好。MCP 工具的粒度应该面向「任务」而非「接口」：与其暴露 <code class="language-plaintext highlighter-rouge">get_user</code>、<code class="language-plaintext highlighter-rouge">get_orders</code>、<code class="language-plaintext highlighter-rouge">get_refunds</code> 三个工具让模型自己编排，不如暴露一个 <code class="language-plaintext highlighter-rouge">get_customer_overview</code>。模型少做一次编排，就少一次出错的机会。</p>

<p><strong>坑三：安全边界缺失。</strong> stdio 模式下 Server 继承宿主进程权限，prompt injection 可以诱导模型调用危险工具。生产实践必须做三件事：工具按最小权限设计（数据库账号只读）、写操作强制人工确认（MCP 协议支持 elicitation）、对工具返回内容做注入检测——工具返回值同样是不可信输入，这一点最容易被忽略。</p>

<p><strong>坑四：忽略协议版本与能力协商。</strong> MCP 在快速演进，2025-03 引入 Streamable HTTP 取代 SSE，2025-06 增强了 OAuth 资源服务器语义。Client 和 Server 在 <code class="language-plaintext highlighter-rouge">initialize</code> 握手时会协商协议版本和能力（capabilities），自研客户端如果硬编码假设对方支持某能力，升级时一定会炸。永远以握手返回的 capabilities 为准。</p>

<h2 id="mcp-不是银弹">MCP 不是银弹</h2>

<p>最后泼点冷水。MCP 解决的是「连接」的标准化，不解决「使用」的智能化。工具接上了，模型用不用得好是另一回事——这取决于工具描述质量、上下文工程和任务分解，这些活儿 MCP 一个都没替你干。</p>

<p>我自己的类比是 HTTP：重要、必要、值得现在就投入，但没有谁靠「支持了 HTTP」赢过。竞争力在协议之上那一层——接进来的东西能不能组合成解决真实问题的能力。</p>

<p>我们内部第一个 MCP Server 上线到现在四个月，最大的收获其实不是技术上的，而是它逼着各团队把「我们的数据能被 AI 怎么用」这个问题想清楚了。工具描述写不出来的接口，多半是接口本身就没想清楚。这个副作用我觉得比协议本身还值。</p>]]></content><author><name>小黄鸭</name></author><category term="AI" /><category term="技术" /><category term="AI" /><category term="MCP" /><category term="Agent" /><category term="协议" /><category term="工程实践" /><summary type="html"><![CDATA[MCP 把「模型如何接入外部世界」从 N×M 的集成噩梦变成 N+M 的标准化问题。本文从协议设计、消息模型到生产实践，完整拆解 MCP 的工程价值与落地陷阱。]]></summary></entry><entry><title type="html">Hermes 多 Agent 工作流：从看到到跑起来</title><link href="http://localhost:4000/ai/%E6%8A%80%E6%9C%AF/2026/05/06/Hermes%E5%A4%9AAgent%E5%B7%A5%E4%BD%9C%E6%B5%81-%E4%BB%8E%E7%9C%8B%E5%88%B0%E5%88%B0%E8%B7%91%E8%B5%B7%E6%9D%A5.html" rel="alternate" type="text/html" title="Hermes 多 Agent 工作流：从看到到跑起来" /><published>2026-05-06T00:00:00+08:00</published><updated>2026-06-13T14:30:46+08:00</updated><id>http://localhost:4000/ai/%E6%8A%80%E6%9C%AF/2026/05/06/Hermes%E5%A4%9AAgent%E5%B7%A5%E4%BD%9C%E6%B5%81-%E4%BB%8E%E7%9C%8B%E5%88%B0%E5%88%B0%E8%B7%91%E8%B5%B7%E6%9D%A5</id><content type="html" xml:base="http://localhost:4000/ai/%E6%8A%80%E6%9C%AF/2026/05/06/Hermes%E5%A4%9AAgent%E5%B7%A5%E4%BD%9C%E6%B5%81-%E4%BB%8E%E7%9C%8B%E5%88%B0%E5%88%B0%E8%B7%91%E8%B5%B7%E6%9D%A5.html"><![CDATA[<p>四月底开始看到各种 Hermes 的 demo：从飞书发一个需求，Agent 军团接着完成市场调研、写 PRD、出架构设计、开发、测试，全程不用人插手。看着确实酷。</p>

<p>花了一个周末把它跑起来，记录一下中间踩过的坑，以及我对这个框架目前能力边界的判断。</p>

<h2 id="hermes-是什么">Hermes 是什么</h2>

<p>从架构上看，Hermes 是一个以「任务看板」（Kanban）为核心的异步多 Agent 框架。跟主流的同步编排框架（Orchestrator 派一批 Worker、等结果收回来）不同，Hermes 的任务是写进持久队列里的，Agent 按状态自己拉任务，跑完再更新状态，支持跨会话、跨天、跨重启继续运行。</p>

<p>理论上这个设计解决了长任务的一个大问题：任务不再绑定在一次会话里，中途可以被人工介入，可以挂起来等外部条件，可以失败后从中间重跑而不是从头。</p>

<p>公式很简单：</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Hermes = 任务队列 + Agent 集合 + 消息总线 + 工具权限管理
</code></pre></div></div>

<p>每个 Agent 有自己的系统提示、工具集、上下文窗口，互相之间通过消息总线通信，不共享上下文。</p>

<h2 id="环境搭建文档和实际有出入">环境搭建：文档和实际有出入</h2>

<p>官方文档写的是几行命令就能起来，实际操作有几个地方文档没说清楚。</p>

<p><strong>模型配置</strong>。Hermes 官方支持接多个模型 API，主流教程用 Kimi K2.6 演示，因为它有官方推荐且开源。我在公司内网环境下，代理设置花了不少时间，文档里没提这部分。</p>

<p><strong>消息总线</strong>。默认用文件系统模拟消息队列（每个 Agent 一个目录，写文件就是发消息），生产环境当然要换成真正的队列（Redis 或者 RabbitMQ），但这个迁移文档写得很粗。本地测试用文件系统就够了，需要知道的是：文件系统的并发写入没有锁，多个 Agent 同时写同一个任务状态文件会出问题，本地测试尽量不要多 Agent 并发跑同一个任务的同一个子步骤。</p>

<p><strong>工具权限</strong>。这是最容易踩坑的地方，也是 demo 里最不演示的地方。Hermes 里每个 Agent 的工具集要显式配置，没有配置的工具 Agent 调不到，调了会报「工具不存在」而不是权限报错，排查很迷惑。配置文件大概是这样：</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">agents</span><span class="pi">:</span>
  <span class="na">researcher</span><span class="pi">:</span>
    <span class="na">tools</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">web_search</span><span class="pi">,</span> <span class="nv">read_file</span><span class="pi">]</span>
    <span class="na">model</span><span class="pi">:</span> <span class="s">kimi-k2</span>
  <span class="na">developer</span><span class="pi">:</span>
    <span class="na">tools</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">read_file</span><span class="pi">,</span> <span class="nv">write_file</span><span class="pi">,</span> <span class="nv">run_bash</span><span class="pi">]</span>
    <span class="na">model</span><span class="pi">:</span> <span class="s">kimi-k2</span>
  <span class="na">reviewer</span><span class="pi">:</span>
    <span class="na">tools</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">read_file</span><span class="pi">,</span> <span class="nv">run_bash</span><span class="pi">]</span>
    <span class="na">model</span><span class="pi">:</span> <span class="s">kimi-k2</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">developer</code> 可以写文件，<code class="language-plaintext highlighter-rouge">reviewer</code> 不能——这个权限边界要自己想清楚，框架不会替你设计。</p>

<h2 id="跑一个真实任务">跑一个真实任务</h2>

<p>用一个我实际跑过的场景说说效果：给一个 Go 的内部工具库写一批单元测试。</p>

<p>任务分发给三个 Agent：</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">analyst</code>：读源码、梳理函数签名和依赖关系、输出测试点清单</li>
  <li><code class="language-plaintext highlighter-rouge">developer</code>：根据清单写测试代码</li>
  <li><code class="language-plaintext highlighter-rouge">reviewer</code>：跑测试、检查覆盖率、输出 review 意见</li>
</ul>

<p>理论上这是多 Agent 的理想场景：任务可清晰切分、各 Agent 产物是下一个的输入、不需要共享上下文。</p>

<p>实际跑下来，前两步基本达到预期，<code class="language-plaintext highlighter-rouge">analyst</code> 出了一份还算准确的清单，<code class="language-plaintext highlighter-rouge">developer</code> 照着写出来的测试能编译，覆盖了七八成的 case。</p>

<p>问题出在 <code class="language-plaintext highlighter-rouge">reviewer</code> 这步。它跑测试发现有几个 case 失败，然后做了一件让我没预料到的事：它直接修改了 <code class="language-plaintext highlighter-rouge">developer</code> 已经写好的测试文件，把 assertion 改成了「更宽松」的版本，让测试通过了。表面上看任务完成了，实际上它在用最省事的方式过关，而不是真正修复逻辑。</p>

<p>这个问题的根因是 <code class="language-plaintext highlighter-rouge">reviewer</code> 的系统提示写得不够严格——「确保测试通过」和「修复测试中的错误」是两件不同的事，但我没有在提示词里区分。多 Agent 系统里，每个角色的职责边界必须在提示词里讲得很死，含糊的空间会被模型用来走捷径。</p>

<p>修完 <code class="language-plaintext highlighter-rouge">reviewer</code> 的提示，加了一句「你只能修改测试文件里的 helper 和 setup 代码，不能修改 assertion，assertion 必须联系 developer agent 修改」，结果就正常了——它开始发消息给 developer，让 developer 来修实现而不是改断言。</p>

<h2 id="和-subagent-模式的比较">和 Subagent 模式的比较</h2>

<p>Hermes 的异步 Kanban 模式和 Claude Code 的 Subagent 模式，适合的场景不一样。</p>

<p>Subagent 适合：任务有明确终点、结果需要在本次会话里拿到、子任务之间依赖关系复杂需要 Orchestrator 实时调度。代码审查、文档生成这类「发出去、等收回来」的任务，Subagent 更顺。</p>

<p>Hermes 适合：任务周期长（几小时到几天）、可能需要人工介入某个环节、各 Agent 之间的协作是持续的而不是单次交付。一个「从需求到上线」的完整开发链路，如果放在 Subagent 里，单次会话跑下来上下文会爆，Hermes 的分阶段持久化就有价值。</p>

<p>不适合 Hermes 的场景也很明确：任务依赖关系是强顺序的（B必须严格等 A 交付后才能开始）、或者任务对实时性要求高（不能等队列调度延迟）。</p>

<h2 id="目前的判断">目前的判断</h2>

<p>跑完这一圈，我觉得 Hermes 现阶段是「思路正确、工程成熟度不足」的状态。任务队列、持久化、多 Agent 消息总线这套设计是对的，解决的是真实的工程问题。但文档的完整度和错误信息的友好度，还停留在「作者自己用着没问题」的阶段。</p>

<p>比较实际的用法：现阶段不要把它用在核心业务链路上，更适合作为「内部效率工具」跑——代码批量处理、文档生成、测试覆盖率提升这类容错性高的场景。出了问题能重跑、不影响线上，这时候 Hermes 的异步持久化优势才能发挥出来，代价也是可以接受的。</p>]]></content><author><name>小黄鸭</name></author><category term="AI" /><category term="技术" /><category term="AI" /><category term="Hermes" /><category term="多Agent" /><category term="工作流" /><category term="工程实践" /><summary type="html"><![CDATA[Hermes（爱马仕）今年在 Agent 圈很热，各种 demo 看着挺酷。花了一个周末把它跑起来，记录一下真实落地和网上教程的差距，以及几个文档里没写清楚的地方。]]></summary></entry><entry><title type="html">CLAUDE.md 多层加载：比你想的复杂</title><link href="http://localhost:4000/ai/%E6%8A%80%E6%9C%AF/2026/04/22/CLAUDE-md%E5%A4%9A%E5%B1%82%E5%8A%A0%E8%BD%BD-%E6%AF%94%E4%BD%A0%E6%83%B3%E7%9A%84%E5%A4%8D%E6%9D%82.html" rel="alternate" type="text/html" title="CLAUDE.md 多层加载：比你想的复杂" /><published>2026-04-22T00:00:00+08:00</published><updated>2026-06-13T14:30:43+08:00</updated><id>http://localhost:4000/ai/%E6%8A%80%E6%9C%AF/2026/04/22/CLAUDE-md%E5%A4%9A%E5%B1%82%E5%8A%A0%E8%BD%BD-%E6%AF%94%E4%BD%A0%E6%83%B3%E7%9A%84%E5%A4%8D%E6%9D%82</id><content type="html" xml:base="http://localhost:4000/ai/%E6%8A%80%E6%9C%AF/2026/04/22/CLAUDE-md%E5%A4%9A%E5%B1%82%E5%8A%A0%E8%BD%BD-%E6%AF%94%E4%BD%A0%E6%83%B3%E7%9A%84%E5%A4%8D%E6%9D%82.html"><![CDATA[<p>事情的起点是一份飘忽不定的报告。</p>

<p>我们内部有个日志分析 Agent，输出格式是 HTML，这个写在那个模块的 CLAUDE.md 里，白纸黑字。结果某天开始变得不稳定：同样的指令，有时候出 HTML，有时候出 Markdown，跑三次三种结果。</p>

<p>第一反应是 prompt 问题。把那个模块的 CLAUDE.md 改到几乎只剩一句「输出格式必须是 HTML」，还是抽奖。查用户级的 <code class="language-plaintext highlighter-rouge">~/.claude/CLAUDE.md</code>，没有相关规则。查子目录，没有。</p>

<p>折腾了两个小时，才想起来：上周我在父目录 <code class="language-plaintext highlighter-rouge">aiworking/</code> 建了一份 CLAUDE.md，用来给底下几个子模块做统筹，里面有一行「分析类报告默认 Markdown」。</p>

<p>目录结构是这样的：</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>aiworking/
    CLAUDE.md          ← 写着「分析类报告默认 Markdown」
    loganalysis/
        CLAUDE.md      ← 我在这里启动 Agent，写着「输出 HTML」
</code></pre></div></div>

<p>我以为只有 <code class="language-plaintext highlighter-rouge">loganalysis/CLAUDE.md</code> 在生效，但实际上两份都进了上下文，Claude 在两条冲突规则之间，看心情选一个。</p>

<h2 id="加载机制是什么">加载机制是什么</h2>

<p>搞清楚这件事之后，我仔细查了 Claude Code 的文档，把加载逻辑整理了一下。</p>

<p><strong>向上遍历</strong>：从你启动 Claude 的目录出发，一级一级往上走，每一级有 CLAUDE.md 就纳入，一直到磁盘根目录为止。<code class="language-plaintext highlighter-rouge">loganalysis/</code> 目录下启动，实际加载路径是：</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>/root/...（各级祖先目录，一般没有）
~/.claude/CLAUDE.md      ← 用户级，固定加载
aiworking/CLAUDE.md      ← 父目录，向上遍历命中
loganalysis/CLAUDE.md    ← 当前目录
loganalysis/CLAUDE.local.md   ← 本地覆盖（如果有）
</code></pre></div></div>

<p><strong>拼接不覆盖</strong>：找到的所有文件不是「近的覆盖远的」，而是全部拼在一起注入上下文，从根到叶顺序排列。所以两条冲突规则同时存在，距离近的排在后面，但不是必然胜出——这取决于 Claude 怎么处理上下文里的矛盾，不保证严格遵守。</p>

<p><strong>子目录懒加载</strong>：这个知道的人更少。启动工作目录下的子目录里如果有 CLAUDE.md，启动时不会加载。要等到 Agent 在会话中真正访问那个子目录里的文件时，该 CLAUDE.md 才会被动态挂载进来。整个会话没有访问那个子目录，它的 CLAUDE.md 不进上下文。</p>

<p>还有一点：CLAUDE.md 不在系统提示词里，它是作为一条用户消息追加在系统提示后面的。这解释了为什么它「不保证严格遵守」——它不是强制配置，Claude 会尽量照做，但遇到矛盾时怎么处理有随机性。</p>

<h2 id="踩坑之后我改了什么">踩坑之后我改了什么</h2>

<p>首先把父目录那条「分析类报告默认 Markdown」删掉，改成了更精确的指向：</p>

<div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">&lt;!-- aiworking/CLAUDE.md --&gt;</span>
适用范围：本目录下各子模块的通用规范

<span class="gu">### 代码规范</span>
<span class="p">-</span> Go 1.22+，禁止使用 init()
<span class="p">-</span> 错误处理用 pkg/errors 包装，禁止裸 fmt.Errorf

<span class="gu">### 注意</span>
各子模块的输出格式以各自的 CLAUDE.md 为准，本文件不约定格式
</code></pre></div></div>

<p>加了最后那句，明确把格式决策权交给子目录，避免父级规则模糊覆盖。</p>

<p>第二个改动是控制规则密度。Augment Code 团队做过一个实验，从一个 monorepo 里提取了几十份 AGENTS.md 逐一测试，发现最好的 AGENTS.md 带来的提升相当于把模型从 Haiku 换成 Opus，而最差的比完全不写还糟。坑在于：同一份文件在常规任务上把质量提升了 25%，在复杂任务上让完整性下降了 30%——写太多规则，规则之间会干扰，Agent 开始在「中间迷失」。</p>

<p>现在我的主文件控制在 100 行以内，只写高频场景的规范；细节推到按需加载的子文件里，在主文件里只写「需要时参考 <code class="language-plaintext highlighter-rouge">@docs/api-rules.md</code>」这样的入口。</p>

<p>第三个改动是清理注释。CLAUDE.md 里的块级 HTML 注释（<code class="language-plaintext highlighter-rouge">&lt;!-- 备注 --&gt;</code>）在注入上下文前会被剥掉，不占 token。所以现在我在文件里保留了大量维护注释，说明每条规则的来源和生效日期，这些注释只给写文件的人看，不会进 Claude 的上下文：</p>

<div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;!-- 
2026-06-15 添加：排查完 loganalysis 输出格式冲突问题后
明确父目录不约定格式，避免向下覆盖子模块规则 
--&gt;
各子模块的输出格式以各自的 CLAUDE.md 为准
</code></pre></div></div>

<h2 id="agentsmd-vs-claudemd">AGENTS.md vs CLAUDE.md</h2>

<p>顺带提一下这两个文件的区别，网上说法很乱。</p>

<p><code class="language-plaintext highlighter-rouge">CLAUDE.md</code> 是 Claude Code 专用的，只有 Claude Code 会去读它。<code class="language-plaintext highlighter-rouge">AGENTS.md</code> 是一个更通用的约定，OpenAI Codex、Cursor、一些开源 Agent 框架都在尝试认这个文件名。</p>

<p>如果你用的是 Claude Code，两个都认，CLAUDE.md 优先。如果你想同时兼容多个工具，用 AGENTS.md。如果只用 Claude Code，CLAUDE.md 就够了，不用两份都维护。</p>

<p>有一个我自己的做法：在项目根目录建一份 <code class="language-plaintext highlighter-rouge">AGENTS.md</code>，内容是通用规范；然后建一个 <code class="language-plaintext highlighter-rouge">CLAUDE.md</code>，内容只有一行：</p>

<div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code>@AGENTS.md
</code></pre></div></div>

<p>这样 Claude Code 读到 CLAUDE.md，拉取 AGENTS.md 的内容，其他工具直接读 AGENTS.md，一份规范两边都能用。这个 symlink 式的做法不是官方推荐的，属于民间发现，用了几个月没出问题。</p>

<h2 id="一句话总结">一句话总结</h2>

<p>CLAUDE.md 的加载是「全量拼接，不是近者优先」。如果你在大型 monorepo 里或者多级目录结构里用 Agent，需要主动管理每一级 CLAUDE.md 的作用域，并且在父级规则里明确标注「以子目录为准」的边界，否则冲突规则会让 Agent 的行为变得不可预测，排查起来很费劲。</p>]]></content><author><name>小黄鸭</name></author><category term="AI" /><category term="技术" /><category term="AI" /><category term="Claude" /><category term="CLAUDE.md" /><category term="AGENTS.md" /><category term="工程实践" /><summary type="html"><![CDATA[以为 CLAUDE.md 只有「用户级 + 项目级」两份？我也这么以为，直到在一个子模块里跑出了薛定谔的输出格式。把踩坑过程和加载机制拆清楚，记录一下。]]></summary></entry></feed>